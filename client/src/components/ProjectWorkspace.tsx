import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { BadgeCheck, ChevronLeft, ChevronRight, CircleDot, ClipboardCheck, Database, FolderKanban, LayoutList, Loader2, Play, Plus, ShieldCheck, Tags, Upload, UsersRound, Wrench } from "lucide-react";
import { useMemo, useState } from "react";
import { toast } from "sonner";
import DataResourceCenter from "./DataResourceCenter";

export type ProjectRecord = { id: string; code: string; name: string; description?: string | null; ownerName?: string | null; ownerUsername?: string | null; workflowCount?: number; createdAt?: string | Date; domainId?: string | null; domainCode?: string | null; domainName?: string | null };

const flowTypeLabel = { state: "状态流程", control: "控制流程", data: "数据流程" } as const;
const flowTypeStyle = { state: "bg-blue-100 text-blue-700", control: "bg-violet-100 text-violet-700", data: "bg-emerald-100 text-emerald-700" } as const;
const auditLabel = { init: "待审核", approved: "审核通过", rejected: "审核驳回" } as const;

function formatDate(value?: string | Date | null) {
  return value ? new Date(value).toLocaleString("zh-CN", { hour12: false }) : "—";
}

export function BusinessCenter({ projects, canCreate, onOpenProject }: { projects: ProjectRecord[]; canCreate: boolean; onOpenProject: (project: ProjectRecord) => void }) {
  const utils = trpc.useUtils();
  const [keyword, setKeyword] = useState("");
  const [showCreate, setShowCreate] = useState(false);
  const [form, setForm] = useState({ code: "", name: "", description: "", domainId: "" });
  const domains = trpc.project.activeDomains.useQuery(undefined, { enabled: canCreate });
  const createProject = trpc.project.create.useMutation({
    onSuccess: result => { void utils.project.list.invalidate(); setShowCreate(false); setForm({ code: "", name: "", description: "", domainId: "" }); toast.success("业务项目已创建。"); },
    onError: error => toast.error(error.message),
  });
  const visible = projects.filter(project => `${project.code} ${project.name} ${project.description ?? ""} ${project.domainCode ?? ""} ${project.domainName ?? ""}`.toLowerCase().includes(keyword.toLowerCase()));
  return <div className="min-h-[calc(100vh-56px)] bg-[#f5f7fb] p-4 sm:p-6">
    <div className="mx-auto max-w-7xl">
      <div className="mb-5 border-b border-slate-200 pb-4"><p className="text-[11px] font-bold tracking-[.16em] text-[#5b72a8]">FLOW DESIGN · BUSINESS CENTER</p><div className="mt-1 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-2xl font-semibold tracking-tight text-slate-800">业务中心</h1><p className="mt-1 text-sm text-slate-500">选择业务项目，进入流程设计中心、项目权限与资源配置工作区。</p></div>{canCreate && <Button className="bg-[#2d6bea] hover:bg-[#255bc8]" onClick={() => setShowCreate(value => !value)}><Plus size={16} />新增业务</Button>}</div></div>
      {showCreate && <form className="mb-5 grid gap-3 rounded-lg border border-[#cbd9f5] bg-white p-4 shadow-sm md:grid-cols-[150px_1fr_1fr_1.2fr_auto]" onSubmit={event => { event.preventDefault(); createProject.mutate({ ...form, domainId: form.domainId || null }); }}><Input placeholder="业务代号，如 OPS" value={form.code} onChange={event => setForm({ ...form, code: event.target.value.toUpperCase() })} required /><Input placeholder="业务名称" value={form.name} onChange={event => setForm({ ...form, name: event.target.value })} required /><select className="h-9 rounded border border-slate-200 bg-white px-2 text-sm" value={form.domainId} onChange={event => setForm({ ...form, domainId: event.target.value })}><option value="">未归属工作域</option>{(domains.data ?? []).map(domain => <option key={domain.id} value={domain.id}>{domain.code} · {domain.name}</option>)}</select><Input placeholder="业务说明（可选）" value={form.description} onChange={event => setForm({ ...form, description: event.target.value })} /><Button disabled={createProject.isPending}>{createProject.isPending && <Loader2 className="animate-spin" size={15} />}保存业务</Button></form>}
      <section className="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-slate-100 bg-slate-50/80 p-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-2 text-sm font-semibold text-slate-700"><FolderKanban size={16} className="text-[#2d6bea]" />业务列表 <span className="text-xs font-normal text-slate-400">（{visible.length}）</span></div><Input className="h-9 max-w-sm bg-white text-sm" placeholder="按业务代号、名称、工作域或说明搜索" value={keyword} onChange={event => setKeyword(event.target.value)} /></div><div className="overflow-x-auto"><table className="w-full min-w-[860px] text-left text-sm"><thead className="bg-[#f8faff] text-xs font-medium text-slate-500"><tr><th className="px-5 py-3">业务代号</th><th className="px-5 py-3">业务名称</th><th className="px-5 py-3">工作域</th><th className="px-5 py-3">流程数量</th><th className="px-5 py-3">创建人</th><th className="px-5 py-3">创建时间</th><th className="px-5 py-3">说明</th><th className="px-5 py-3 text-right">操作</th></tr></thead><tbody>{visible.map(project => <tr key={project.id} className="border-t border-slate-100 transition-colors hover:bg-blue-50/40"><td className="px-5 py-4 font-mono text-xs font-semibold text-[#2d6bea]">{project.code}</td><td className="px-5 py-4 font-medium text-slate-800">{project.name}</td><td className="px-5 py-4 text-xs text-slate-500">{project.domainCode ? <span className="rounded bg-violet-50 px-2 py-1 text-violet-700">{project.domainCode} · {project.domainName}</span> : "未归属"}</td><td className="px-5 py-4 text-slate-500">{Number(project.workflowCount ?? 0)}</td><td className="px-5 py-4 text-slate-500">{project.ownerName || project.ownerUsername || "—"}</td><td className="px-5 py-4 text-xs text-slate-500">{formatDate(project.createdAt)}</td><td className="max-w-[260px] truncate px-5 py-4 text-slate-500">{project.description || "—"}</td><td className="px-5 py-4 text-right"><button className="font-medium text-[#2d6bea] hover:text-[#174fb5]" onClick={() => onOpenProject(project)}>进入业务 <ChevronRight className="inline" size={14} /></button></td></tr>)}{!visible.length && <tr><td colSpan={8} className="px-5 py-12 text-center text-sm text-slate-400">暂无可访问业务项目。{canCreate ? "请新建业务后开始设计流程。" : "请联系管理员授予项目角色。"}</td></tr>}</tbody></table></div></section>
    </div>
  </div>;
}

type WorkspaceView = "process" | "members" | "resources";
export function ProjectWorkspace({ project, onBack, onOpenWorkflow, onOpenWarehouse }: { project: ProjectRecord; onBack: () => void; onOpenWorkflow: (workflowId: string) => void; onOpenWarehouse: () => void }) {
  const utils = trpc.useUtils();
  const [view, setView] = useState<WorkspaceView>("process");
  const [filters, setFilters] = useState<{ flowType?: "state" | "control" | "data"; auditStatus?: "init" | "approved" | "rejected"; status?: "draft" | "published"; keyword?: string }>({});
  const [showCreate, setShowCreate] = useState(false);
  const [detailWorkflowId, setDetailWorkflowId] = useState<string | null>(null);
  const [form, setForm] = useState({ name: "", description: "", flowType: "state" as "state" | "control" | "data" });
  const access = trpc.project.access.useQuery({ projectId: project.id });
  const workflows = trpc.project.workflows.useQuery({ projectId: project.id, ...filters });
  const members = trpc.project.members.useQuery({ projectId: project.id });
  const detailWorkflow: any = trpc.workflow.get.useQuery({ id: detailWorkflowId ?? "00000000" }, { enabled: Boolean(detailWorkflowId) });
  const canManage = Boolean(access.data?.permissions?.has("project:manage"));
  const canCreate = Boolean(access.data?.permissions?.has("project:workflow:create"));
  const createWorkflow = trpc.project.createWorkflow.useMutation({
    onSuccess: (workflow: any) => { void utils.project.workflows.invalidate({ projectId: project.id }); void utils.project.list.invalidate(); setShowCreate(false); setForm({ name: "", description: "", flowType: "state" }); toast.success("项目流程已创建，待审核后可发布。"); if (workflow?.id) onOpenWorkflow(workflow.id); },
    onError: error => toast.error(error.message),
  });
  const auditWorkflow = trpc.project.auditWorkflow.useMutation({ onSuccess: () => { void utils.project.workflows.invalidate({ projectId: project.id }); toast.success("审核状态已更新。"); }, onError: error => toast.error(error.message) });
  const runWorkflow = trpc.workflow.run.useMutation({ onSuccess: result => toast.success(`流程已发起：${result.runId.slice(0, 8)}`), onError: error => toast.error(error.message) });
  const runDataflow = trpc.data.run.useMutation({ onSuccess: result => { void utils.data.runs.invalidate({ projectId: project.id }); toast.success(`数据流运行完成：${result.runId.slice(0, 8)}`); }, onError: error => toast.error(error.message) });
  const menu = [{ id: "process" as const, label: "流程设计中心", icon: LayoutList }, { id: "members" as const, label: "权限配置中心", icon: ShieldCheck }, { id: "resources" as const, label: "资源配置中心", icon: Database }];
  return <div className="flex min-h-[calc(100vh-56px)] flex-col bg-[#f5f7fb] lg:flex-row"><aside className="w-full shrink-0 border-b border-[#dbe2ef] bg-white lg:w-64 lg:border-b-0 lg:border-r"><div className="border-b border-slate-100 p-4"><button className="flex items-center gap-1 text-xs text-[#2d6bea] hover:underline" onClick={onBack}><ChevronLeft size={14} />返回业务中心</button><p className="mt-4 text-[10px] font-bold tracking-[.16em] text-slate-400">CURRENT BUSINESS</p><h2 className="mt-1 truncate text-base font-semibold text-slate-800">{project.name}</h2><p className="mt-1 font-mono text-[11px] text-slate-400">{project.code}</p></div><nav className="grid gap-1 p-2">{menu.map(item => <button key={item.id} onClick={() => setView(item.id)} className={`flex items-center gap-2 rounded-md px-3 py-2.5 text-left text-sm transition-colors ${view === item.id ? "bg-[#eaf1ff] font-semibold text-[#245fc8]" : "text-slate-600 hover:bg-slate-50"}`}><item.icon size={16} />{item.label}{item.id === "members" && <span className="ml-auto text-[10px] text-slate-400">{members.data?.length ?? 0}</span>}</button>)}</nav></aside><section className="min-w-0 flex-1 p-4 sm:p-6">{view === "process" && <ProcessCenter project={project} workflows={(workflows.data ?? []) as any[]} filters={filters} setFilters={setFilters} showCreate={showCreate} setShowCreate={setShowCreate} form={form} setForm={setForm} canCreate={canCreate} canManage={canManage} creating={createWorkflow.isPending} onCreate={() => createWorkflow.mutate({ projectId: project.id, ...form })} onAudit={(workflowId, auditStatus) => auditWorkflow.mutate({ projectId: project.id, workflowId, auditStatus })} onOpenWorkflow={onOpenWorkflow} onOpenWarehouse={onOpenWarehouse} onLaunch={workflow => workflow.flowType === "data" ? runDataflow.mutate({ projectId: project.id, workflowId: workflow.id }) : runWorkflow.mutate({ workflowId: workflow.id, input: {} })} onDetail={setDetailWorkflowId} />}{view === "members" && <ProjectMembers projectId={project.id} members={(members.data ?? []) as any[]} canManage={canManage} />}{view === "resources" && <DataResourceCenter projectId={project.id} onOpenWorkflow={onOpenWorkflow} />}</section>{detailWorkflow.data && <div className="fixed inset-0 z-50 flex items-end justify-center bg-slate-950/30 p-3 sm:items-center"><section className="w-full max-w-xl rounded-lg bg-white p-5 shadow-2xl"><div className="flex items-start justify-between gap-4"><div><p className="text-[10px] font-bold tracking-[.16em] text-[#5b72a8]">PROCESS DETAIL</p><h2 className="mt-1 text-lg font-semibold text-slate-800">{detailWorkflow.data.name}</h2></div><button className="text-sm text-slate-400 hover:text-slate-700" onClick={() => setDetailWorkflowId(null)}>关闭</button></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><DetailItem label="流程类型" value={flowTypeLabel[(detailWorkflow.data as any).flowType as keyof typeof flowTypeLabel] ?? "状态流程"} /><DetailItem label="审核状态" value={auditLabel[(detailWorkflow.data as any).auditStatus as keyof typeof auditLabel] ?? "待审核"} /><DetailItem label="发布状态" value={detailWorkflow.data.status === "published" ? "已发布" : "未发布"} /><DetailItem label="定义版本" value={`v${detailWorkflow.data.definitionVersion}`} /></div><p className="mt-4 rounded bg-slate-50 p-3 text-sm leading-6 text-slate-600">{detailWorkflow.data.description || "未填写流程说明"}</p><Button className="mt-5" onClick={() => { setDetailWorkflowId(null); onOpenWorkflow(detailWorkflow.data.id); }}>进入设计器</Button></section></div>}</div>;
}

function ProcessCenter({ project, workflows, filters, setFilters, showCreate, setShowCreate, form, setForm, canCreate, canManage, creating, onCreate, onAudit, onOpenWorkflow, onOpenWarehouse, onLaunch, onDetail }: { project: ProjectRecord; workflows: any[]; filters: any; setFilters: (value: any) => void; showCreate: boolean; setShowCreate: (value: boolean) => void; form: { name: string; description: string; flowType: "state" | "control" | "data" }; setForm: (value: { name: string; description: string; flowType: "state" | "control" | "data" }) => void; canCreate: boolean; canManage: boolean; creating: boolean; onCreate: () => void; onAudit: (id: string, status: "approved" | "rejected") => void; onOpenWorkflow: (id: string) => void; onOpenWarehouse: () => void; onLaunch: (workflow: any) => void; onDetail: (id: string) => void }) {
  return <div><div className="mb-5 flex flex-col gap-3 border-b border-slate-200 pb-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-[11px] font-bold tracking-[.16em] text-[#5b72a8]">{project.code} · PROCESS DESIGN CENTER</p><h1 className="mt-1 text-xl font-semibold text-slate-800">流程设计中心</h1><p className="mt-1 text-sm text-slate-500">管理状态、控制和数据三类流程的审核、发布和执行生命周期。</p></div><div className="flex flex-wrap gap-2"><Button variant="outline" onClick={onOpenWarehouse}><Upload size={15} />上传仓库</Button>{canCreate && <Button className="bg-[#2d6bea] hover:bg-[#255bc8]" onClick={() => setShowCreate(!showCreate)}><Plus size={16} />新增流程</Button>}</div></div>{showCreate && <form className="mb-5 grid gap-3 rounded-lg border border-[#cbd9f5] bg-white p-4 shadow-sm md:grid-cols-[1fr_1.3fr_150px_auto]" onSubmit={event => { event.preventDefault(); onCreate(); }}><Input placeholder="流程名称" value={form.name} onChange={event => setForm({ ...form, name: event.target.value })} required /><Input placeholder="流程说明（可选）" value={form.description} onChange={event => setForm({ ...form, description: event.target.value })} /><select className="h-9 rounded-md border border-slate-200 bg-white px-2 text-sm" value={form.flowType} onChange={event => setForm({ ...form, flowType: event.target.value as typeof form.flowType })}><option value="state">状态流程</option><option value="control">控制流程</option><option value="data">数据流程</option></select><Button disabled={creating}>{creating && <Loader2 className="animate-spin" size={15} />}创建并设计</Button></form>}<div className="mb-4 grid gap-2 rounded-lg border border-slate-200 bg-white p-3 sm:grid-cols-4"><Input className="h-8 text-xs" placeholder="搜索流程名称或说明" value={filters.keyword ?? ""} onChange={event => setFilters({ ...filters, keyword: event.target.value || undefined })} /><select className="h-8 rounded border border-slate-200 px-2 text-xs" value={filters.flowType ?? ""} onChange={event => setFilters({ ...filters, flowType: event.target.value || undefined })}><option value="">全部流程类型</option><option value="state">状态流程</option><option value="control">控制流程</option><option value="data">数据流程</option></select><select className="h-8 rounded border border-slate-200 px-2 text-xs" value={filters.auditStatus ?? ""} onChange={event => setFilters({ ...filters, auditStatus: event.target.value || undefined })}><option value="">全部审核状态</option><option value="init">待审核</option><option value="approved">审核通过</option><option value="rejected">审核驳回</option></select><select className="h-8 rounded border border-slate-200 px-2 text-xs" value={filters.status ?? ""} onChange={event => setFilters({ ...filters, status: event.target.value || undefined })}><option value="">全部发布状态</option><option value="draft">未发布</option><option value="published">已发布</option></select></div><section className="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[1040px] text-left text-sm"><thead className="bg-[#f8faff] text-xs font-medium text-slate-500"><tr><th className="px-4 py-3">流程代号</th><th className="px-4 py-3">流程名称 / 说明</th><th className="px-4 py-3">类型</th><th className="px-4 py-3">审核状态</th><th className="px-4 py-3">发布状态</th><th className="px-4 py-3">创建人</th><th className="px-4 py-3">更新时间</th><th className="px-4 py-3 text-right">操作</th></tr></thead><tbody>{workflows.map(workflow => <tr key={workflow.id} className="border-t border-slate-100 hover:bg-blue-50/40"><td className="px-4 py-3 font-mono text-xs text-[#2d6bea]">{String(workflow.id).slice(0, 8).toUpperCase()}</td><td className="px-4 py-3"><p className="font-medium text-slate-800">{workflow.name}</p><p className="mt-0.5 max-w-[230px] truncate text-xs text-slate-400">{workflow.description || "未填写流程说明"}</p></td><td className="px-4 py-3"><span className={`rounded px-2 py-1 text-xs ${flowTypeStyle[workflow.flowType as keyof typeof flowTypeStyle] ?? "bg-slate-100 text-slate-600"}`}>{flowTypeLabel[workflow.flowType as keyof typeof flowTypeLabel] ?? workflow.flowType}</span></td><td className="px-4 py-3"><span className={`text-xs ${workflow.auditStatus === "approved" ? "text-emerald-700" : workflow.auditStatus === "rejected" ? "text-red-600" : "text-amber-600"}`}><CircleDot className="mr-1 inline" size={12} />{auditLabel[workflow.auditStatus as keyof typeof auditLabel] ?? "待审核"}</span></td><td className="px-4 py-3"><span className={`rounded border px-2 py-1 text-xs ${workflow.status === "published" ? "border-blue-200 bg-blue-50 text-blue-700" : "border-slate-200 text-slate-500"}`}>{workflow.status === "published" ? "已发布" : "未发布"}</span></td><td className="px-4 py-3 text-xs text-slate-500">{workflow.creatorName || workflow.creatorUsername || "—"}</td><td className="px-4 py-3 text-xs text-slate-500">{formatDate(workflow.updatedAt)}</td><td className="px-4 py-3 text-right whitespace-nowrap"><button className="mr-3 text-xs text-slate-600 hover:underline" onClick={() => onDetail(workflow.id)}>详情</button><button className="mr-3 text-xs font-medium text-[#2d6bea] hover:underline" onClick={() => onOpenWorkflow(workflow.id)}>设计</button>{workflow.status === "published" && <button className="mr-3 text-xs text-[#245fc8] hover:underline" onClick={() => onLaunch(workflow)}>发起</button>}{canManage && workflow.auditStatus === "init" && <><button className="mr-2 text-xs text-emerald-700 hover:underline" onClick={() => onAudit(workflow.id, "approved")}>通过</button><button className="text-xs text-red-600 hover:underline" onClick={() => onAudit(workflow.id, "rejected")}>驳回</button></>}</td></tr>)}{!workflows.length && <tr><td colSpan={8} className="px-4 py-14 text-center text-sm text-slate-400">项目内暂无匹配流程。{canCreate ? "可从右上角新建状态、控制或数据流程。" : ""}</td></tr>}</tbody></table></div></section></div>;
}

function DetailItem({ label, value }: { label: string; value: string }) { return <div className="rounded border border-slate-100 bg-slate-50 p-3"><p className="text-[10px] font-bold tracking-[.12em] text-slate-400">{label}</p><p className="mt-1 text-sm font-medium text-slate-700">{value}</p></div>; }

function ProjectMembers({ projectId, members, canManage }: { projectId: string; members: any[]; canManage: boolean }) {
  const utils = trpc.useUtils();
  const users = trpc.iam.users.useQuery(undefined, { enabled: canManage });
  const [form, setForm] = useState({ userId: "", role: "viewer" as "owner" | "designer" | "operator" | "viewer", hours: "" });
  const grant = trpc.project.grantMember.useMutation({ onSuccess: () => { void utils.project.members.invalidate({ projectId }); toast.success("项目成员授权已保存。"); setForm({ userId: "", role: "viewer", hours: "" }); }, onError: error => toast.error(error.message) });
  return <div><div className="mb-5 border-b border-slate-200 pb-4"><p className="text-[11px] font-bold tracking-[.16em] text-[#5b72a8]">PROJECT AUTHORIZATION</p><h1 className="mt-1 text-xl font-semibold text-slate-800">项目权限配置中心</h1></div>{canManage && <form className="mb-5 grid gap-3 rounded-lg border border-[#cbd9f5] bg-white p-4 md:grid-cols-[1fr_160px_180px_auto]" onSubmit={event => { event.preventDefault(); const userId = Number(form.userId); if (userId) grant.mutate({ projectId, userId, role: form.role, expiresAt: form.hours ? new Date(Date.now() + Number(form.hours) * 3600_000) : undefined }); }}><select className="h-9 rounded border border-slate-200 px-2 text-sm" value={form.userId} onChange={event => setForm({ ...form, userId: event.target.value })} required><option value="">选择内部账号</option>{(users.data ?? []).filter(user => user.status === "active").map(user => <option key={user.id} value={user.id}>{user.name || user.username}（{user.username}）</option>)}</select><select className="h-9 rounded border border-slate-200 px-2 text-sm" value={form.role} onChange={event => setForm({ ...form, role: event.target.value as typeof form.role })}><option value="viewer">查看者</option><option value="operator">运行者</option><option value="designer">设计者</option><option value="owner">项目所有者</option></select><Input className="h-9" type="number" min="1" placeholder="临时有效期小时（可选）" value={form.hours} onChange={event => setForm({ ...form, hours: event.target.value })} /><Button disabled={grant.isPending}><UsersRound size={15} />授予成员</Button></form>}<section className="overflow-hidden rounded-lg border border-slate-200 bg-white"><div className="border-b border-slate-100 px-4 py-3 text-sm font-semibold">项目角色列表</div><div className="divide-y divide-slate-100">{members.map(member => <div key={member.id} className="flex flex-col gap-2 px-4 py-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-medium text-slate-800">{member.name || member.username}</p><p className="mt-0.5 text-xs text-slate-400">{member.username} · 生效：{formatDate(member.effectiveFrom)} · 到期：{member.expiresAt ? formatDate(member.expiresAt) : "长期"}</p></div><span className="w-fit rounded bg-[#edf3ff] px-2 py-1 text-xs font-medium text-[#245fc8]">{member.role}</span></div>)}{!members.length && <p className="p-8 text-center text-sm text-slate-400">项目尚无成员。</p>}</div></section></div>;
}

function ResourcePreview() { return <div className="rounded-lg border border-dashed border-slate-300 bg-white p-8"><Wrench className="text-slate-300" size={32} /><h2 className="mt-4 font-semibold text-slate-700">资源配置中心</h2><p className="mt-2 max-w-xl text-sm leading-6 text-slate-500">已恢复原始项目工作区的资源配置入口。数据源、UDF、标签和项目插件将按已确认的 P2 顺序接入，并与当前项目和权限边界绑定。</p><div className="mt-5 flex flex-wrap gap-2 text-xs text-slate-500"><span className="rounded bg-slate-100 px-2 py-1"><Database className="mr-1 inline" size={13} />数据源</span><span className="rounded bg-slate-100 px-2 py-1"><Wrench className="mr-1 inline" size={13} />UDF / 插件</span><span className="rounded bg-slate-100 px-2 py-1"><Tags className="mr-1 inline" size={13} />标签中心</span></div></div>;
}
