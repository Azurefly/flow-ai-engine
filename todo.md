# Project TODO

- [x] 核验恢复后的仓库状态、数据库结构和可用资产，确认此前实现是否存在可恢复副本。
- [x] 重建流程管理、节点画布、节点配置、导入导出和后端自主执行能力。
- [x] 重建 LLM、HTTP、条件分支和数据转换节点及真实数据运行测试。
- [x] 重建内部账号、密码哈希、会话、用户状态与管理员引导能力。
- [x] 重新验证管理员引导、登录、注销、创建账号、禁用用户及旧会话失效。
- [x] 将 Drizzle schema 与保留数据库的 auth_session、permission、角色和流程表结构完全对齐。
- [x] 重建系统角色、自定义角色、流程成员、临时角色、权限审计和最后所有者保护。
- [x] 将流程创建、查看、编辑、发布、运行、日志查看与成员管理绑定到资源级权限。
- [x] 重建登录页、用户与权限中心、流程内成员管理以及权限感知设计器交互。
- [x] 执行认证、授权、隔离、导入导出、真实 HTTP 节点与响应式回归验证。
- [x] 保存可恢复版本并完成最终交付。
- [x] 使 Drizzle Schema 精确映射保留数据库的 auth_session、role_assignment、permission、审计和执行记录字段。
- [x] 实现系统权限目录、内置角色初始化、工作流成员与有效期角色授权服务。
- [x] 将工作流读取、编辑、发布与成员管理从“仅所有者”策略切换为资源级权限判定。
- [x] 重新生成并审查 Drizzle 迁移，使 `drizzle/` 产物与新的 Schema 及保留数据库契约一致。
- [x] 实现自定义角色 CRUD、权限绑定与相应授权边界测试。
- [x] 为自定义角色创建、更新、权限绑定、有效授权阻止删除与授权撤销边界补充真实数据库回归测试。
- [x] 实现后端工作流执行引擎：变量插值、开始、LLM、HTTP、转换、条件与结束节点，并保证不依赖浏览器逐节点推进。
- [x] 为 HTTP 节点实现协议、DNS、环回及私有地址拦截、禁用重定向与响应体限制。
- [x] 将工作流运行、节点日志、输入输出、授权快照与耗时写入保留数据库，并提供资源权限保护的运行及日志 API。
- [x] 在流程仓库完成复制、删除、成员列表与授权可见性操作，并以资源级权限保护这些管理动作。
- [x] 在流程仓库补全可审阅成员列表，展示用户、角色、生效期、到期时间和撤销状态。
- [x] 为流程复制、删除和成员查看补充有权限成功、无权限拒绝及删除关联数据清理的回归测试。
- [x] 将无效登录凭据从内部服务器错误改为明确的未授权响应，并补充路由层回归测试。
- [x] 使用运行时模型目录完成一次真实 LLM 节点调用验证，并记录模型选择与响应结果。
- [x] 为登录、登出、创建用户和禁用用户写入授权审计记录并完成真实数据库测试。
- [x] 实现流程运行与运行日志 API，并绑定运行及日志查看的资源级权限。
- [x] 为 workflow.run、workflow.runs 与 workflow.runDetail 覆盖有权限成功、无权限拒绝、日志可见性及授权快照落库的可重复测试。
- [x] 为 IAM 的授权边界、临时授权有效期、所有者保护和审计记录补充可重复执行的测试。
- [x] 为包含真实 LLM 节点的完整工作流补充运行时模型选择、节点输出与 workflow_node_run 落库集成测试。
- [x] 补充管理员引导与用户禁用后旧会话访问被拒绝的真实数据库回归测试。
- [x] 在认证审计集成测试中显式断言 user_created 与 user_disabled 事件已写入授权审计表。
- [x] 第 1 轮：核验构建、开发服务、现有自动化测试基线与数据库连接。
- [x] 第 2 轮：验证管理员引导、登录失败语义、登出和会话撤销。
- [x] 第 2 轮补充：使用独立 bootstrap 身份验证管理员引导创建，并验证登出后的旧 Cookie 无法调用受保护路由。
- [x] 第 3 轮：验证禁用用户、旧会话失效和认证审计完整性。
- [x] 第 4 轮：验证系统角色、自定义角色、临时授权和最后所有者保护。
- [x] 第 5 轮：验证流程创建、复制、删除、成员授予/撤销和资源隔离。
- [x] 第 5 轮补充：验证成员授予、撤销、临时有效期、角色范围及无成员管理权限拒绝。
- [x] 第 5 轮增强：断言 expiresAt 落库、到期授权失效，并覆盖 owner/editor/operator/viewer 角色范围。
- [x] 第 5 轮最终：插入已过期流程成员记录，验证 workflow.get、workflow.run 和 workflow.members 均拒绝访问。
- [x] 第 6 轮：验证 JSON 导入导出、定义持久化与流程版本更新。
- [x] 第 6 轮补充：断言 definitionVersion 递增，并从路由层验证导入定义的保存和导出一致性。
- [x] 第 7 轮：验证 HTTP、转换、条件分支和结束节点的真实后端自主执行。
- [x] 第 8 轮：验证运行时模型目录、真实 LLM 节点、节点日志和运行历史。
- [x] 第 9 轮：验证 SSRF 防护、权限拒绝、输入边界和安全错误语义。
- [x] 第 9 轮补充：验证非法流程定义、成员参数、运行输入和节点配置的校验错误语义。
- [x] 第 9 轮最终：断言 workflow.run 非对象输入、空对象与成员非法角色/到期时间的严格错误类型与消息。
- [x] 第 9 轮消息断言：校验 workflow.run 非对象输入与 grantMember 非法角色的 BAD_REQUEST 明确消息。
- [x] 第 10 轮：验证设计器交互、成员管理界面、桌面/移动响应式及全量回归。
- [x] 第 10 轮补充：使用隔离测试管理员登录主控制台，验证设计器、运行中心、成员授权界面和窄屏布局。
- [x] 修复运行详情路由仅接受 UUID 导致历史非 UUID 运行记录无法在运行中心加载节点日志的问题，并补充回归测试。
- [x] 清理身份与权限中心中可识别的历史测试账号、临时流程与关联审计记录，避免验收数据污染真实管理目录。
- [x] 汇总十轮缺陷、修复、复测证据并保存新的可恢复交付版本。
- [x] 第 10 轮最终：已登录控制台窄屏布局、浏览器导出触发与受确认的成员授予/撤销交互验证。
- [x] 修复流程仓库新建按钮未提交表单的问题，并补充浏览器与回归验证。
- [x] 修复 375 像素已登录设计器的横向溢出，使工具栏、协作成员、运行输入、画布和节点检查器可在窄屏单列访问。
- [x] 设计并迁移工作流历史版本、版本差异与安全回滚的数据模型，保留创建者、来源版本和完整定义快照。
- [x] 实现受资源级权限保护的版本列表、结构化差异比较与一键回滚服务，并审计回滚动作。
- [x] 扩展运行中心，提供按状态、时间和触发者筛选、耗时指标及失败率统计。
- [x] 实现持久化的工作流失败告警、已读状态和资源隔离，并向项目所有者发送失败运行操作通知。
- [x] 实现流程节点模板库，支持从现有节点保存个人模板、按类型复用、编辑和删除。
- [x] 实现可复用子流程定义及子流程节点执行，在调用处保留可审计的输入输出和失败上下文。
- [x] 在参考式控制台中新增版本治理、运行分析/告警及模板/子流程交互界面，并保持窄屏可访问。
- [x] 为版本回滚、运行分析、失败告警、节点模板和子流程调用补充真实数据库与权限边界回归测试。
- [x] 执行扩展后的全量类型检查、自动化测试、浏览器验收并保存新的可恢复交付版本。
- [x] 补充模板配置编辑后再次复用的真实数据库与浏览器闭环验证。
- [x] 针对版本治理、运行分析/告警和复用资产面板完成 375px 窄屏浏览器验收，并记录无横向溢出证据。
- [x] 排查已部署环境的初始管理员无法登录问题，核验引导环境变量、账号状态、密码哈希与登录 API 错误语义。
- [x] 修复线上初始管理员认证链路，完成真实部署登录与会话验证并保存新的可恢复版本。
- [x] 清理线上管理员登录验收中发现的残留集成测试流程、运行数据、成员、版本和随机测试账号。
- [ ] 完成 AiFlowGraph 原始安装包的页面、菜单、节点、配置、运行和管理功能基线，并与逐文件清单保持一致。
- [x] 完成参考目录原始静态前端、后端源码、安装配置和资源文件的逐文件功能审计与恢复优先级确认。
- [x] P0：恢复原始顶层四页签、业务中心及项目工作区壳层，保留内部认证、IAM 和响应式约束。
- [x] P0：建立项目/流程分类数据模型及项目成员权限，支持状态、控制、数据三类流程的创建、筛选和生命周期状态。
- [x] P0：恢复项目流程设计中心的表格、审核/发布状态、仓库上传、发起/启动与详情入口。
- [x] P0：恢复状态/控制流程画布的原始节点库、画布工具、属性配置和流程引导，并与安全执行引擎兼容。
- [x] P0：恢复树形流程仓库、目录操作、流程移动、只读画布预览、简介编辑及批量导入导出。
- [x] P1：恢复实例任务工作台、我的看板、日历、待办/已办/发起/全部流程、实例详情和人工操作。
- [x] P1：恢复系统配置、审批流配置、系统水印和工作域管理，并使用管理员权限保护。
- [x] P2：恢复数据源、资源探查、数据流任务与调度、UDF、标签和项目插件资源域。
- [ ] 按原始基线分阶段执行数据库迁移、权限测试、真实数据浏览器验收与检查点保存。
- [x] P0：恢复真实顶层流程仓库和系统配置工作区壳层，替换当前占位或 IAM 复用页面。
- [x] P0：为流程设计中心补齐上传仓库、按类型发起/启动和独立流程详情入口，并覆盖权限边界。
- [x] P0：为已发布项目流程发起/启动补充真实数据库与浏览器闭环，覆盖成功运行、未发布阻止和缺少项目运行权限拒绝。
- [x] P0：为 state/operate/router/rest/form/sql 节点实现安全执行分派，或在运行前进行明确的设计态阻断校验。
- [x] P0：为项目仓库导入补充项目级权限和浏览器验证，证明“上传仓库”不是单纯页面跳转。
- [x] P0：修复流程仓库“打开设计器”在全局流程列表未刷新时错误回退到首条流程的问题，并覆盖真实浏览器路径。
- [x] P0：为 router 节点补充分支句柄真实数据库测试，验证仅执行所选后继边。
- [x] P1：设计并迁移流程实例任务、待办处理、人工操作结果和任务审计的项目隔离数据模型。
- [x] P1：实现待办、已办、我发起和全部流程实例的资源级查询、任务领取/完成与实例详情 API。
- [x] P1：将 operate 节点从安全阻断升级为可暂停的内部人工任务，并在任务完成后由服务端继续执行后继节点。
- [x] P1：恢复已启动流程工作台的我的看板、日历、待办、已办、发起、全部和实例详情界面，吸纳原始导航结构。
- [x] P1：持久化系统通用设置、审批规则、系统水印和工作域配置，并由管理员权限保护。
- [x] P1：为 P1 任务隔离、人工操作续跑、系统设置权限及浏览器工作台闭环补充真实数据库与浏览器回归测试。
- [x] P1：修复我的看板在同一人工任务既为待办又为我发起时的重复展示，并重新完成浏览器任务闭环验收。
- [x] P1：修复人工任务续跑后实例总耗时未包含等待处理时长的问题，并执行回归测试。
- [x] 回归：更新 P0 原始流程集成测试以匹配 P1 operate 人工任务语义，并在清理流程运行前删除关联 workflow_task。
- [x] P1：将通用设置应用到控制台名称和系统水印渲染，并完成浏览器验证。
- [x] P1：让审批配置实际驱动项目流程发布门禁，并补充真实数据库测试。
- [x] P1：将工作域关联到业务项目与工作区查询展示，证明工作域不是孤立目录表。
- [x] P1：修复通用设置保存后公共控制台名称与水印查询未失效的问题，并完成浏览器复验。
- [x] 回归：更新项目工作区测试中的发布门禁错误断言，以匹配可配置审批规则的明确语义。
- [x] P2：建立项目隔离的数据源、资源探查、UDF、标签、项目插件、数据流定义和数据流运行数据模型，并迁移真实数据库。
- [x] P2：实现数据资源中心 API 与项目成员资源级权限，覆盖数据源、探查、UDF、标签和插件的隔离增删改查。
- [x] P2：恢复原始式数据流工作区，支持数据流设计、手动运行、运行历史和项目资源引用。
- [x] P2：实现托管后台计划任务的数据流调度、暂停/恢复/删除、幂等回调和运行审计；实际启用前保存检查点并提示用户发布。
- [x] P2：为数据资源隔离、数据流执行、调度回调和浏览器工作区补充真实数据库与浏览器闭环验收。
- [x] 回归：确保 P2 数据源列表不返回凭据引用或连接配置中的潜在秘密字段。
- [x] 回归：修复新建或仓库导入流程的异步详情加载后画布未随 definition 刷新的残留节点问题，并完成浏览器复验。
- [x] 回归：将数据流设计器内的后端运行入口路由至 P2 数据流执行引擎并显示对应运行审计。
- [x] 回归：修复调度草稿已入库但浏览器 tRPC 请求网关超时的问题，并保留计划变更授权审计。
- [x] P2：核验已启用“P2 浏览器订单数据流”的首个后台运行审计；平台于 `2026-08-19T09:09:04Z` 投递并在 `09:09:10Z` 返回 HTTP 200，写入真实 `dataflow_run`。生产时间桶复验仍由独立发布后任务跟踪。
- [ ] P2：在托管计划激活后验证暂停、恢复、删除和非法回调 403，以及按可信任务 UID 的幂等运行审计；当前已完成生产计划暂停/恢复、非法 403、真实应用级隔离删除及数据库幂等集成测试，仍待当前发布版本下的真实时间桶幂等复验。
- [x] P2 证据补全：分别记录真实已激活计划的暂停/恢复、隔离计划删除、非法回调 403 和幂等回调的验收边界；在不影响生产计划前提下决定是否执行真实删除验收。
- [x] P2 发布后验收：激活 DEMO5A6A 项目的“P2 浏览器订单数据流”托管计划，并核验任务 UID 与激活状态持久化。
- [ ] P2 发布后验收：生产首个可信计划回调已返回 2xx 并写入 dataflow_run 审计；发布包含 `scheduleBucket` 的当前版本后，复验重复回调幂等行为与非法请求 403。
- [x] P2 发布后补充真实非法回调 403 复验：在当前发布版本下，以无计划身份请求 `/api/scheduled/dataflow`，确认返回 403 且不新增 `dataflow_run`。
- [ ] P2 发布后补充真实时间桶幂等复验：取得当前可信 `taskUid + minute bucket` 的重复或重放回调证据，确认只保留一条 `dataflow_run`，不能以单次成功运行替代。
- [ ] P2 发布后验收：验证托管计划暂停、恢复和删除操作与平台任务状态一致；当前生产计划已完成暂停/恢复，隔离数据流计划已完成应用记录与平台任务同时删除，仍待当前发布版本下的完整生命周期一致性复验。
- [x] 安装包前端还原：逐页提取原始 AiFlowGraph 页面结构、菜单、工具栏、列表列、弹窗、状态文案与交互入口，形成可审查的对照清单。
- [ ] 安装包前端还原：恢复顶层四页签、业务中心、流程设计中心、已启动流程、仓库、系统配置与资源中心的原始可见细节和操作状态。
- [ ] 节点配置原始证据补全：取得缺失的 `bundle.js`、画布脚本或完整归档后，逐字段确认开始、结束、状态、操作、路由、REST、表单、SQL、转换、条件、LLM、子流程及数据流节点的原始属性字段、默认值、帮助文案和校验规则。
- [x] 节点配置还原：将完整属性面板与流程定义持久化、版本化、导入导出和安全执行映射逐项对齐，禁止出现仅有节点外观而缺少配置的情况。
- [x] 节点属性编辑稳定性：修复草稿字段变动导致画布重建/定义同步循环的问题，并在真实浏览器中验证 REST 请求方法编辑后选中节点和高级 JSON 配置均保持同步。
- [x] 原始证据补全：建立包含页面、脚本、后端源码、安装配置和资源文件的逐文件审计表，逐项标注恢复优先级、证据来源和当前映射位置。
- [x] P0 细节补全：恢复并测试状态/控制流程的禁用业务资源与物理资源入口、流程引导区块及原始详情页可见操作。
- [x] 补回流程详情区的“查看详细配置”显式操作入口，并以真实浏览器验证能定位到节点属性面板。
- [x] 为流程详情“已启动流程列表”补回原始“操作”列（实例详情或打开运行记录），并加入界面回归与浏览器验收。
- [x] 原始节点证据补全：将每个原始节点字段、默认值、帮助文案和校验规则回链到安装包证据；缺失打包脚本无法证明的字段必须标为扩展字段。
- [x] 节点配置证据修正：当前裁剪副本无法直接证明的字段、默认值、帮助文案和校验规则已在证据矩阵中统一标记为兼容扩展，不再宣称为原始安装包字段。
- [x] 节点闭环验收：逐节点验证配置面板字段与定义持久化、版本快照、导入导出、安全执行器及数据流执行器字段读取的一致性。
- [x] 节点配置还原：补充原始节点配置的真实数据库、定义 JSON、执行分派和浏览器编辑回归测试。
- [x] 原始流程操作恢复：为已启动流程补回任务移交与可审计的操作回退入口，使用资源级权限和状态机边界替代历史控制器的无隔离实现。
- [x] 原始流程操作恢复：为授权用户补回受限批量领取/完成处理，逐项返回结果并保证失败不越权、不跨项目处理。
- [x] 原始证据审计：将裁剪副本中唯一历史控制器的可见流程操作、模板与状态接口逐项映射到当前实现或明确标记为不安全而不复刻。
- [x] 原始工作区细节恢复：为项目工作区补回可收起侧栏与当前受权限业务上下文提示，并完成桌面及窄屏浏览器验收。
- [x] 原始业务中心细节恢复：补回创建时间范围、查询/重置分离和“同步 BDP 配置”受管入口，不连接未经确认的外部系统。
- [x] 原始已启动流程细节恢复：为流程工作台补回可收起左栏与当前视图上下文提示，并完成不改变任务状态的浏览器验收。
- [x] 原始流程详情操作恢复：补回“取消发布”操作，要求具备资源级发布权限、保留版本/运行审计且不删除已有实例记录。
- [x] 原始流程设计中心细节恢复：为类型、审核、发布和关键词筛选补回显式“重置筛选”操作，并完成浏览器验收。
- [x] 原始流程设计中心证据补全：恢复可证明的流程描述、创建时间、发布/取消时间列表列及被驳回流程的受权审核重置语义；完整字段仍以缺失 bundle 复核为准。
- [x] 原始流程时间审计恢复：为取消发布增加独立 `unpublishedAt` 持久化字段，并在项目流程查询中安全返回创建、发布和取消时间，不以 `updatedAt` 替代。
- [x] 原始流程时间审计恢复：为取消发布增加独立 `unpublishedAt` 持久化字段，并在项目流程查询中安全返回创建、发布和取消时间，不以 `updatedAt` 替代。
- [x] 原始流程审核操作恢复：为被驳回的项目流程补回“重置审核状态”，仅允许项目管理权限执行，写入审计且恢复为待审核草稿而不自动发布。
- [x] 原始流程详情细节恢复：在项目流程详情弹层展示真实流程说明、创建、发布与取消时间，并完成不改变流程状态的浏览器验收。
- [x] 回归：修复项目切换或工作区复用时筛选状态残留，避免真实存在的授权流程偶发显示为空列表，并补充界面回归。
- [x] 安装包仓库细节恢复：为左侧目录树补回关键词过滤、搜索无结果提示和只读预览工具状态，并完成界面回归。
- [x] P2 生命周期实测：生产计划已完成短暂停止、平台状态核验与恢复启用；非法生产回调真实返回 403 且未生成运行审计。
- [x] P2 删除闭环：为隔离的数据流托管计划从应用记录发起端到端删除，核对数据库计划状态与平台任务列表同时移除，且不影响生产计划。
- [x] 配置体验重构：所有面向用户的节点、流程、数据流与系统配置均改为结构化表单、选择器和可重复字段编辑，不要求用户直接填写 JSON；仅保留隐藏或受限的兼容迁移通道并补充回归验收。
- [x] 本轮原始界面证据补全：在流程设计中心列表展示流程说明、创建时间、最近发布与取消发布时间，并完成项目工作区窄屏浏览器验收。
- [x] 原始数据流详情细节恢复：在资源中心以真实运行审计展示操作 ID、业务名称、流程名称、启动时间、结束时间与详情操作，并保持窄屏表格可访问。
- [x] 原始已启动流程细节恢复：为实例详情补回可关闭的工作台内动态页签，维持运行权限边界且不绕过既有运行中心。
- [x] 原始已启动流程细节恢复：补回首次读取工作台统计时的页面加载遮罩，避免空数据误导且不阻塞后续后台刷新。
- [x] 原始数据流画布细节恢复：为资源中心补回“保存为图片”和“整理画布”的显式工具按钮，并安全引导至受权限保护的设计器。
- [x] 原安装包视觉基线审计：为业务中心、项目工作区、流程设计器、已启动流程、流程仓库、系统配置和资源中心建立同尺寸参考图、布局尺寸、色彩、工具栏和状态差异清单。
- [ ] 原安装包壳层高保真还原：依据原始静态页面恢复顶层导航、左右分栏、面板边框、标题栏、工具条、列表密度和空状态的统一视觉语言，不改变鉴权或业务数据语义。
- [ ] 原安装包逐页高保真还原：按差异清单逐页修复业务中心、项目工作区、流程设计器、已启动流程、流程仓库、系统配置和资源中心，并完成真实数据的视觉验收。
- [x] 原安装包工作区状态还原：在项目流程查询期间展示明确加载反馈，避免异步授权数据尚未返回时误显示“暂无匹配流程”。
- [x] 回归修复：移除数据资源中心遗留的 JSON 文本编辑器与 JSON.parse 路径，仅保留结构化数据源和资源探查表单。
- [x] 原安装包流程详情状态补强：在已启动流程列表查询期间显示明确加载行，避免表头下出现无状态空白区。
- [x] 原安装包已启动流程壳层稳定化：以左导航稳定 aria 标记锚定卡片页签与右侧内容区样式，替换泛化结构选择器依赖。
- [x] 原始业务中心操作恢复：补回受权限保护的“导入业务”入口，使用列式 CSV 预校验并复用内部项目创建契约，不要求用户编辑 JSON。
- [x] 回归修复：修正数据流画布辅助工具区的自定义属性写法，消除 React 非布尔属性警告。
- [x] 回归修复：删除数据资源中心未挂载的遗留 JSON 状态、解析和表单代码，仅保留结构化资源表单提交路径。
- [x] 回归修复：删除未挂载的人工任务 JSON 文本抽屉，确保用户处理任务仅使用结构化字段行。
