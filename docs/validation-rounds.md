# Flow AI Engine 审查—修改—审查台账

本台账仅记录实际执行过的数据库集成测试、类型检查、已登录浏览器闭环或平台托管任务核验。页面源码断言不能替代真实数据库验证；生产计划首跑在下一次 UTC 触发后另行补记。

| 轮次 | 变更或审查重点 | 真实核验范围 | 结果与证据 |
|---:|---|---|---|
| 1 | 内部账号与会话基线 | 管理员引导、密码哈希、登录/注销 | `auth-bootstrap-session`、`internal-auth`、`auth.logout` 集成测试。 |
| 2 | IAM 与资源权限 | 系统角色、流程角色、临时授权、最后所有者保护 | `iam-*`、`workflow-permissions`、`workflow-owner-protection` 数据库测试。 |
| 3 | 自主工作流执行 | LLM 目录、HTTP SSRF 拒绝、节点运行审计 | `workflow-engine*`、`workflow-llm` 集成测试。 |
| 4 | P0 项目工作区 | 项目隔离、审核、发布、仓库归档 | `project-workspace`、`project-original-flow` 集成测试。 |
| 5 | 路由与人工操作 | 分支句柄、人工待办、服务端续跑 | `workflow-engine`、`p1-task` 集成测试。 |
| 6 | P1 系统设置 | 审批门禁、工作域、水印和管理员权限 | `p1-system-config` 集成测试。 |
| 7 | P2 数据资源域 | 项目隔离、资源探查、只读 SQL 与运行审计 | `p2-resource-dataflow` 集成测试。 |
| 8 | 设计器统一节点契约 | 草稿/执行边界、默认值、旧式路由/表单/结束模板兼容 | `workflow-node-contract`、`workflow-definition-validation`。 |
| 9 | 设计器重建 | 原始工具栏、配置状态、字段化属性面板与模板入口 | `workflow-ui-regression` + 类型检查。 |
| 10 | 流程详情与数据流工具 | 流程引导、资源/函数树、任务详情、调度入口 | `workflow-ui-regression`、`p2-resource-dataflow`。 |
| 11 | 托管计划生命周期 | 真实创建可信任务 UID、暂停—恢复、活动状态持久化 | 数据库 `dataflow_schedule`、平台任务 UID `LZdLD7JML8zvtPWtiXFEj8`、`manus-heartbeat list`。 |
| 12 | 计划回调幂等 | task UID + UTC 分钟桶唯一运行键 | `dataflow_run.scheduleBucket` 迁移、唯一索引与 P2 数据库回归。 |
| 13 | 全量并行稳定性 | 审批全局设置污染定位并修复 | 命名锁后 `pnpm test`：24 文件、41 项通过。 |
| 14 | 系统配置页面细节 | 左栏收起/展开、卡片页签，设置能力无回归 | `p1-system-config`、`workflow-ui-regression` 与全量回归。 |
| 15 | 节点配置持久化 | 开始、状态、操作、路由、REST、结束字段的保存、重读、导出、复制 | `workflow-definition-persistence` 真实数据库测试；全量 24 文件、41 项通过。 |
| 16 | 流程详情实例列表 | 真实运行查询、关键词筛选、实例状态表与版本治理共存 | `workflow-governance`、`workflow-ui-regression`，全量 24 文件、41 项通过。 |
| 17 | 托管计划回调闭环 | 可信 UID、运行审计写入、同分钟重复回调幂等、非法 403、隔离计划删除 | `p2-schedule-callback` 真实数据库集成测试；全量 **25** 文件、**42** 项通过。 |
| 18 | 节点属性编辑稳定性 | REST 临时节点的 `POST → GET` 字段编辑、检查器选中状态与高级 JSON 即时同步 | 浏览器真实验收定位并修复草稿 key 重建及定义同步循环；类型检查与 14 项画布/节点契约回归通过。 |
| 19 | 扩展节点配置持久化 | 表单、转换、条件、LLM、私有子流程和 HTTP 的完整配置，以及开始、状态、操作、路由、REST、结束节点配置 | `workflow-definition-persistence` 创建真实私有子流程并验证保存、重读、导出与复制定义完全一致；执行分派、路由与 SSRF 由 `workflow-engine*`、`project-original-flow`、`workflow-governance` 覆盖；全量 **25** 文件、**42** 项通过。 |
| 20 | 历史流程操作恢复 | 任务移交、退回待处理、逐项批量领取/完成、运行续跑与操作审计 | `p1-task` 真实数据库测试创建项目隔离的两位处理人及外部用户，验证可分配人过滤、移交、退回、批量处理、审计与运行完成；已登录浏览器仅查看真实待办详情，确认批量工具栏、授权处理人下拉框与移交入口可见，未执行任何会改变真实任务状态的操作。 |
| 21 | 生产托管计划预检 | 已激活任务、计划路径、下次触发时间与历史执行记录 | 非破坏性 `manus-heartbeat list/logs` 核验：`LZdLD7JML8zvtPWtiXFEj8` 保持 active，路径为 `/api/scheduled/dataflow`，计划为 `0 0 9 * * *` UTC，下一次为 `2026-08-19T09:00:00Z`；当前尚无平台执行记录。 |
| 22 | 原始业务中心与工作区细节 | 创建时间范围、查询/重置、受管同步入口、可收起业务侧栏 | 已登录浏览器显示三条授权业务、日期输入及查询/重置控件；BDP 同步保持禁用且无外部请求。进入 `DEMO5A6A` 后收起工作区导航，图标菜单、项目代号和流程表仍可访问；窄屏已由响应式代码覆盖，待带登录会话的窄屏浏览器补充截图。 |
| 23 | 原始流程详情取消发布 | 资源级发布权限、取消发布、版本快照、运行记录保留与浏览器治理入口 | 真实数据库测试验证外部用户拒绝、设计者取消发布、既有运行保留、`unpublished` 草稿版本与审计写入、后续运行阻断；浏览器真实显示发布治理与取消发布控件，未执行状态变更。 |
| 24 | 原始审核重置语义 | 已驳回未发布流程、项目管理权限、待审核草稿、审计与重复操作阻断 | `project-workspace` 真实数据库测试验证项目设计者被拒绝、项目所有者可将 rejected draft 重置为 init draft、定义保留且记录 `workflow_audit_reset` 审计；重复重置被阻断。流程详情已接入受 `project:manage` 保护的确认入口并纳入界面回归；未在真实验收流程上触发状态变更。 |
| 25 | 原始时间审计恢复 | 创建时间、最近发布时间、最近取消发布时间与取消发布写入 | 迁移为 `workflow.unpublishedAt` 增加独立可空字段；真实数据库取消发布测试验证发布时间清空、取消时间写入且运行记录保留；浏览器在已发布流程详情显示三项时间，不以更新时间替代。 |
| 26 | 原始流程筛选操作 | 类型、审核、发布、关键词筛选的显式查询与重置 | 流程设计中心改为本地筛选草稿，只有点击“查询”才应用到已授权项目查询；“重置筛选”同步清空草稿及已应用条件。类型检查、界面回归和真实浏览器均通过。 |
| 27 | 托管计划首跑窗口核验 | 已启用计划、09:00 UTC 触发窗口与平台投递日志 | 在 `2026-08-19T09:00:54Z` 与 `09:04:01Z` 非破坏性查询 `LZdLD7JML8zvtPWtiXFEj8`：任务保持启用，但 `next_execution_at` 未推进且 `runs` 仍为空。当前无平台投递证据，不能宣称生产回调或数据流审计成功；需确认生产发布与平台计划投递状态。 |
| 28 | 生产首跑实际结果 | 平台 2xx、回调运行 ID、计划触发字段与幂等时间桶 | 平台随后在 `09:09:04Z` 投递一次回调，`09:09:10Z` 返回 HTTP 200 与 `{ "ok": true, "runId": "cJ2glbU_1eLdO2xS" }`；数据库已写入 `triggerType=schedule`、`lastTriggeredAt` 和 `lastRunId`。但该生产运行的 `scheduleBucket` 为 SQL NULL，表明线上仍在使用未包含时间桶传递逻辑的旧发布版本；生产幂等复验必须在发布当前修复版本后重新执行。当前 `p2-schedule-callback` 真实数据库集成测试已再次通过，确认待发布代码会写入可信任务时间桶并拒绝非法及重复回调。 |
| 29 | 项目流程详情时间 | 流程说明、创建、发布与取消时间的详情展示 | 已发布控制流程的真实项目详情弹层展示创建时间、最近发布时间和未取消发布状态；字段由 `createdAt`、`publishedAt`、`unpublishedAt` 驱动。列表级时间列仍作为独立恢复项待补。 |
| 30 | 项目工作区筛选重置 | 项目切换后真实流程可见性 | 发现项目进入后的初始渲染可短暂显示空表；网络日志确认列表请求最终返回两条授权流程。已在 `project.id` 变化时重置筛选草稿、已应用筛选、创建面板和详情选择，完成类型检查、项目工作区集成测试、界面回归与真实浏览器复验。 |
| 31 | 生产计划持续状态 | 启用状态、成功执行与后续触发窗口 | 只读平台任务查询确认 `LZdLD7JML8zvtPWtiXFEj8` 仍为启用，回调路径为 `/api/scheduled/dataflow`，上次成功执行为 `2026-08-19T09:09:10Z`，下一次为 `2026-08-20T09:00:00Z`。未暂停、恢复、删除或手动运行生产计划；`scheduleBucket` 修复仍须在当前版本发布后复验。 |
| 32 | 流程仓库目录搜索 | 目录路径保留、无结果状态与只读边界 | 目录树的关键词过滤现递归保留命中目录及其父路径。已登录浏览器在 `DEMO5A6A` 输入“已发布”后保留“已发布流程”目录，中间根目录列表正确显示无匹配提示；未触发任何仓库写操作。类型检查、界面回归及全量 **25** 个测试文件、**43** 项测试通过。 |
| 33 | 生产非法计划回调 | 无计划身份拒绝与运行审计副作用 | 对线上 `/api/scheduled/dataflow` 发送不带计划身份的 POST 请求，真实返回 HTTP 403。随后只读查询 `dataflow_run`，仍仅存在此前 `cJ2glbU_1eLdO2xS` 的一条计划运行，未新增运行记录。 |
| 34 | 生产计划暂停—恢复 | 平台生命周期状态与下次触发窗口 | 生产任务 `LZdLD7JML8zvtPWtiXFEj8` 已真实短暂停用，列表核验 `is_enable=false`；随后立即恢复，返回启用成功和 `next_execution_at=2026-08-20T09:00:00Z`，最终列表再次确认 `is_enable=true`。未删除生产任务，隔离删除与当前版本发布后的时间桶幂等复验仍待完成。 |
| 35 | 隔离计划删除 | 真实删除语义与生产计划隔离 | 创建远期、不绑定数据流业务行的隔离计划 `LRSfWWFrXHZfF5kJk49T2U` 后立即调用删除；平台返回成功。后续列表仅保留生产任务 `LZdLD7JML8zvtPWtiXFEj8`，且其 `is_enable=true`、下一次触发仍为 `2026-08-20T09:00:00Z`。由真实数据库 `p2-schedule-callback` 测试覆盖同分钟时间桶幂等和回调隔离；当前生产版本写入时间桶仍待发布后复验。 |
| 36 | 应用级隔离计划删除 | 应用记录、平台任务与生产计划隔离 | 显式开启真实集成测试后，系统创建隔离项目与已发布数据流，通过 `saveDataflowScheduleDraft`、`activateDataflowSchedule` 创建远期平台任务，确认任务 UID 在平台列表存在；再通过 `deleteDataflowSchedule` 删除，确认 `dataflow_schedule.status='deleted'` 且平台任务消失。测试清理完成后平台列表只保留生产任务 `LZdLD7JML8zvtPWtiXFEj8`，其状态仍为启用。 |
| 37 | 结构化配置首批改造 | 节点配置与数据资源录入 | 节点 JSON 型属性改为结构化键值、列表、路由规则与表单字段编辑器，并隐藏用户侧高级 JSON 兼容区；资源中心的数据源与资源探查改为普通字段、可重复字段结构和样本键值对。类型检查、界面回归、真实浏览器验收及全量 **25** 个测试文件、**43** 项测试通过；真实平台删除测试在默认回归中按显式开关安全跳过。运行输入、人工处理结果、调度表达式和其余配置入口仍按新需求继续盘点改造。 |
| 38 | 结构化运行输入 | 流程运行字段编辑与类型保持 | 流程设计器将 `id`、`prompt` 初始输入解析为可编辑键值对，并在数值、`true` / `false` 输入时保留相应基础类型后提交原有运行对象契约。类型检查、11 项界面回归、已登录浏览器验收及全量 **25** 个测试文件、**44** 项测试通过；真实平台删除测试在默认回归中按显式开关安全跳过。未触发运行、保存或发布。人工任务完成结果和调度表达式仍待继续改造。 |
| 39 | 结构化人工处理结果 | 任务字段编辑、逐项完成与服务端续跑契约 | 已挂载任务抽屉改为“处理结果字段”的可重复键值编辑器，数值与布尔值保持基础类型后交给既有 `task.complete` / `task.batchComplete` 服务；原 JSON 抽屉保留为未挂载兼容实现。类型检查、11 项界面回归及全量 **25** 个测试文件、**44** 项测试通过。已登录工作台当前无可见待办，故仅验证工作台空状态，未执行领取、完成、移交或退回。 |
| 40 | 用户配置表单化闭环 | 节点、模板、运行、人工处理、数据资源与系统设置 | 用户可编辑的节点复杂字段、运行输入、人工处理结果、数据源、资源探查、通用设置、审批与工作域均已采用字段化控件、选择器或可重复行。节点高级配置与模板 JSON 配置不向用户展示；导入/导出和运行审计中的 JSON 仅作为受控文件交换或只读技术展示保留。结构化节点、资源、运行与任务输入均已通过类型检查、界面回归、真实浏览器或真实数据库回归的相应覆盖；后续全量回归为 **25** 个测试文件、**44** 项通过，真实平台删除测试文件在默认环境按显式开关安全跳过。 |
| 41 | 生产时间桶复验前核验 | 平台状态、最近执行与运行审计字段 | 只读平台列表确认生产任务 `LZdLD7JML8zvtPWtiXFEj8` 仍启用，上次平台执行为 `2026-08-19T09:09:10Z`，下次为 `2026-08-20T09:00:00Z`。只读数据库查询仅见一次计划成功运行 `cJ2glbU_1eLdO2xS`，其 `scheduleBucket` 仍为 NULL；因此线上尚未执行包含当前时间桶传递逻辑的发布版本，不能将此视为生产幂等闭环。 |
| 42 | 流程设计中心时间列与工作区窄屏 | 原始流程说明、创建、发布/取消发布列；当前业务提示与可收起导航 | 以已登录真实受权项目 `P0CBF7DE` 验证两条流程显示真实创建、最近发布/取消发布和更新时间；未发布项明确显示“尚未发布”，未取消项显示“尚未取消发布”。在保留登录态的 `375×812` 设备视口中，DOM 实测文档宽 `360px`、表格滚动容器 `326px` 且 `overflow-x:auto`、表格宽 `1380px`，页面主体无横向溢出；收起工作区导航后上下文提示、图标菜单、筛选与流程数据保持可访问。`pnpm check` 通过；全量 `pnpm test` 为 **25** 个文件通过、**1** 个受显式开关保护的真实平台删除测试文件跳过，合计 **44** 项通过、**1** 项跳过。 |
| 43 | 原始数据流详情操作列表 | 原始操作 ID、业务、流程、启动/结束时间、操作；真实数据流运行审计 | 依据 `dataFlowCanvas.html` 与 `dataflow_process_detail.html`，资源中心“数据流”页补入可横向浏览的“已操作流程列表”及只读“查看审计”。已登录 `DEMO5A6A` 真实浏览器显示两条既有 `dataflow_run`：计划运行 `cJ2glbU_` 与手动运行 `tzgZk4gW`，其启动/结束时间、触发类型、触发人和成功状态均与服务端审计一致；展开详情只读取结果、耗时与状态，未执行运行或调度变更。类型检查及全量 `pnpm test` 均通过：**25** 文件、**44** 项通过，真实平台删除测试文件按显式开关跳过。 |
| 44 | 已启动流程动态实例详情页签 | 原始 `initiatedProcess.js` 的实例详情卡片创建、激活与关闭语义 | 工作台“实例详情”不再离开工作台跳转至全局运行中心，而是在同一工作台内以可关闭页签展示同一资源级 `workflow.runDetail` 数据；关闭或“返回工作台”仅清除本地选中状态。已完成类型检查、界面回归和全量自动化回归：**25** 文件、**44** 项通过，真实平台删除测试文件按显式开关跳过。当前登录会话的待办、我发起及全部流程均为零；虽然数据库存在其他受隔离用户/项目的历史实例，但未创建测试数据、未借用账号也未绕过权限进行浏览器点击，因此该详情页签的有数据浏览器复验保留为授权会话可用时的后续证据。 |
| 45 | 已启动流程首次加载遮罩 | 原始 `initiatedProcess.html` 的 `loadingMask` 页面加载反馈 | 工作台首次读取看板统计时显示“正在读取已启动流程”与当前授权范围加载提示，替代旧实现中在查询未完成时展示零计数的歧义；数据到达后仅显示真实授权数据，后续后台刷新不遮挡当前视图。开发预览真实会话完成连接和授权项目加载后稳定显示四个授权业务。类型检查、界面回归与全量自动化回归通过：**25** 文件、**44** 项通过，真实平台删除测试文件按显式开关跳过。 |
| 46 | 数据流画布辅助工具入口 | 原始 `dataFlowCanvas.html` 的保存图片与整理画布工具 | 在 `DEMO5A6A` 真实数据流页确认资源树、函数树、交并差/SQL/TSML/中间表、任务详情、调度配置、运行审计以及“保存为图片 / 整理画布”按钮均可见。点击“保存为图片”仅导航到当前受权限保护的 `P2 浏览器订单数据流` 设计器，并显示明确提示；没有点击设计器内保存、发布、运行或计划操作。修复无值自定义数据属性的 React 控制台警告后，类型检查、界面回归与全量回归均通过：**25** 文件、**44** 项通过，真实平台删除测试文件按显式开关跳过。 |
| 47 | 发布后可信计划时间桶与非法回调 | 当前已发布生产回调、可信任务 UID、分钟时间桶持久化、403 无副作用 | 只读数据库核验确认发布后新计划运行 `N1MTG5qcSmhD8_XL` 于 `2026-08-20 09:08:25 UTC` 成功写入 `scheduleBucket=LZdLD7JML8zvtPWtiXFEj8:2026-08-20T09:08`；该桶当前查询到一条运行记录，证明当前版本的时间桶传递已生效。旧发布版本运行 `cJ2glbU_1eLdO2xS` 保持 `scheduleBucket=NULL`，仅作为修复前历史证据。随后向当前生产 `/api/scheduled/dataflow` 发送不带计划身份的最小 POST，返回 HTTP 403 与 `permission error for cron cookie`；前后计划运行总数均为 **2**、最近运行仍为 `2026-08-20 09:08:25`，无副作用。尚未获得当前版本同一分钟的重复或重放回调，故不能将此轮视作完整生产幂等闭环；本轮未暂停、恢复、删除或手动触发现有生产任务。 |
| 48 | 原安装包视觉基线第一阶段 | 顶层壳层、业务中心、工作区、设计器、已启动流程、仓库与资源中心的真实页面收敛 | 以裁剪安装包静态页面可证明结构为边界，将控制台由深色仪表盘式外观收敛为白色标题栏、薄灰分隔线、低圆角面板与连续工作区；业务中心、项目工作区、流程仓库、已启动流程和数据流资源中心均已用真实受权数据浏览器复验。字段化配置、资源级权限与数据流审计不变。全量 `pnpm test` 结果为 **25** 文件通过、**1** 个显式开关保护的真实平台删除测试文件跳过，合计 **45** 项通过、**1** 项跳过。完整 CSS/图标资源及原始动态脚本仍缺失，故本轮不宣称像素级完整还原。 |
| 49 | 原安装包主画布高保真收敛 | 原始 `design_process.html` 的标题、双工具条、主画布与配置面板骨架 | 以 1280px 真实受权数据流设计器核验：默认收起新增的流程仓库侧栏，保留全局按钮按需展开；画布主区显示原始可证明的开始/结束/REST/SQL/状态/操作/路由/子流程等节点工具、禁用资源项、整理/图片/全屏/取消高亮工具、配置状态图例、操作提示和配置面板。工具条改为纵向图标+文字、薄分隔线，节点卡片改为低圆角无阴影。业务中心亦按原静态页收敛为紧凑标题、筛选条和连续表格。类型检查、界面回归及全量 `pnpm test` 通过：**25** 文件、**45** 项通过；真实平台删除测试文件按显式开关跳过。原安装包引用的完整 CSS、图标和 `bundle.js` 仍不可访问，故保留像素级对照及逐页细节还原为待办。 |
| 50 | 生产计划执行历史只读复验 | 当前可信任务 UID、平台成功响应与重复/重放回调痕迹 | 只读查询 `LZdLD7JML8zvtPWtiXFEj8` 的平台执行历史：当前仅有两次各 `attempts=1` 的成功投递，分别为 `2026-08-19T09:09:04Z` 和 `2026-08-20T09:08:21Z`；后者返回 `{ "ok": true, "runId": "N1MTG5qcSmhD8_XL" }`。未出现当前版本同一分钟内的重试、重复或重放记录，故生产幂等闭环仍不能标记完成。本轮没有暂停、恢复、删除、修改或手动触发现有生产计划。 |
| 51 | 当前时间桶数据库只读复验 | `dataflow_run` 计划运行、可信分钟桶与唯一审计记录 | 只读查询显示两条计划运行：`N1MTG5qcSmhD8_XL` 成功且 `scheduleBucket=LZdLD7JML8zvtPWtiXFEj8:2026-08-20T09:08`；修复前历史运行 `cJ2glbU_1eLdO2xS` 的 `scheduleBucket=NULL`。当前可信时间桶只存在一条记录，符合首跑写入但不构成重复回调幂等的生产证明。查询过程不写入或更改数据库。 |
| 52 | 回调幂等测试时钟边界 | 顺序可信回调的同分钟桶断言 | 全量回归中发现定向测试在单次数据流运行耗时跨分钟时会把第二次请求误归为新桶；测试现固定应用时钟至同一 UTC 分钟。定向回调测试和全量测试均通过：**25** 文件、**45** 项通过、**1** 项安全跳过。只读生产任务历史仍为两次跨日单次成功运行，未触发、暂停、恢复或删除生产计划；该测试稳定性修复不构成生产重复回调证明。 |
| 53 | 结构化配置源码清理 | 节点检查器、模板管理和运行输入 | 删除流程设计器中仅由 CSS 隐藏的高级节点 JSON 编辑器、模板 JSON 提示框和顶部运行输入文本框。模板配置须在画布中使用原有字段化检查器调整后另存；运行按钮已置入字段名/字段值表单。真实受权 `P2 浏览器订单数据流` 设计器 DOM 复核 `textarea=[]`，并保留四个结构化运行字段输入。类型检查、界面回归和全量测试通过：**25** 文件、**45** 项通过、**1** 项安全跳过；未保存、发布或执行流程。 |
| 54 | 设计器画布优先层级 | 原始 `design_process.html` 的标题栏、节点工具、画布和右侧配置面板顺序 | 原始静态页面明确为标题栏后直接呈现节点工具与画布，再显示配置信息。本轮以稳定 `data-aiflow-designer` 标记调整直系布局顺序，使真实 `P2 浏览器订单数据流` 页面在标题栏之后立即显示纵向节点/画布工具、配置状态、开始—结束画布和右侧检查器；运行字段与权限辅助信息保持可访问但位于画布之后。未保存、发布、运行、成员授权或撤销。界面回归、类型检查及全量 `pnpm test` 均通过：**25** 文件、**45** 项通过、**1** 项安全跳过。 |
| 55 | 流程仓库紧凑上下文 | 原始 `processWarehouse.html` 的左树—内容/预览连续分栏 | 原始页面仅将“流程列表”置于左面板标题栏，项目级上下文并非独立营销介绍区。当前仓库将项目选择收敛为 40px 紧凑条，保留项目切换，移除大标题与说明占用；真实 `DEMO5A6A` 页面验证左侧搜索、批量操作、目录树，中间真实三条流程及右侧流程简介/只读画布空选择态均可见，连续分隔线三栏保持。未导入、导出、创建目录、移动流程或打开设计器。界面回归、类型检查及全量 `pnpm test` 均通过：**25** 文件、**45** 项通过、**1** 项安全跳过。 |
| 56 | 数据流资源工作台紧凑化 | 原始 `dataFlowCanvas.html` 的资源树、顶部工具、数据流工具和运行记录 | 原始 DOM 明确数据资源/函数资源、`一键展开`、`任务详情`、`调度配置`、保存画布、交集/差集/并集/SQL/TSML/中间表以及保存图片/整理画布。当前资源中心隐藏非原始大标题，将数据流参考壳层的介绍压缩为 40px 工具条；真实 `DEMO5A6A` 页面确认资源 1、函数资源、全部原始工具入口、三条真实运行审计（含 `N1MTG5qc` 当前版本计划运行）、已发布数据流、托管计划与结构化调度草稿均可见。未运行、暂停、删除、保存草稿、打开设计器或修改资源。界面回归、类型检查及全量 `pnpm test` 均通过：**25** 文件、**45** 项通过、**1** 项安全跳过。 |
## 当前尚待记录的生产验收

| 验收项 | 条件 | 通过标准 |
|---|---|---|
| 首次托管回调 | 已发布当前版本并获得可信计划回调。 | 已完成：`N1MTG5qcSmhD8_XL` 返回成功、时间桶已写入。 |
| 重复回调幂等 | 获得当前版本同一分钟的重投递或重放回调。 | 尚待复验：单次成功桶不能替代重复回调证据。 |
| 非法回调拒绝 | 当前发布版本下，无计划身份请求 `/api/scheduled/dataflow`。 | 已完成：HTTP 403，计划运行总数不变。 |
| 已登录浏览器编辑 | 使用内部账号登录最新发布版本。 | 配置面板字段编辑、保存、刷新、导入/导出与运行日志一致。 |
