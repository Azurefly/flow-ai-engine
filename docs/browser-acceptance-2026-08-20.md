# 2026-08-20 浏览器验收记录

## 已确认的桌面基线

在已登录的开发预览中，业务中心列出 4 个当前已授权项目，并保留业务代号、名称、工作域、流程数量、创建人、创建时间、说明和进入业务操作。进入 `DEMO5A6A` 的“原始工作区验收项目”后，项目工作区显示返回入口、可收起导航、当前业务代码与名称，以及流程设计、权限配置和资源配置三个受权限约束的区域。

流程设计中心的表头已显示“流程名称 / 说明”“创建时间”“最近发布时间”“最近取消发布时间”和“更新时间”。该项目目前无可见流程，因此本轮仍需在一个存在已授权流程的项目中核验时间值和窄屏布局；当前验收没有执行保存、发布、运行、审核或调度操作。

随后在 `P0CBF7DE` 的“P0 原始流程验收”项目中，以真实受权数据核验了双流程列表。已发布流程显示真实创建时间 `2026/8/18 02:58:59` 与最近发布时间 `2026/8/18 02:59:07`，取消发布时间明确显示“尚未取消发布”；未发布流程的最近发布时间与取消发布时间分别显示“尚未发布”和“尚未取消发布”。页面没有将更新时间冒充为发布或取消发布时间。该核验仅查看数据，未触发发起、审核、发布、保存或调度操作。

通过保留登录态的浏览器设备仿真切换到 `375×812` 后，`P0CBF7DE` 工作区的“当前业务”上下文提示、可收起导航、流程筛选控件与两条受权流程数据均可读取，时间审计列保留在可横向浏览的列表中。加载完成前短暂显示空列表，后续查询成功恢复两条记录；该阶段应继续以 DOM 尺寸核验表格的横向滚动是否被外层容器正确收纳。

DOM 实测显示：视口宽度为 `375px`，文档与 `body` 宽度均为 `360px`；流程表格外层滚动容器宽度为 `326px`、`overflow-x: auto`，表格自身宽度为 `1380px`。因此时间审计表的横向内容仅由表格容器滚动承载，页面主体没有横向溢出。继续点击“收起项目工作区导航”后，按钮切换为“展开项目工作区导航”，图标菜单与当前业务提示保留，筛选控件和两条流程记录仍可访问；未执行任何改变业务数据的操作。
