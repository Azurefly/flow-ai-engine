# P0 浏览器验收记录

本轮验收使用内部管理员账号进入“原始工作区验收项目”（`DEMO5A6A`）的流程仓库。该项目在开始时没有根目录流程，但保留了“已发布流程”目录。

已准备最小可执行状态流程导入文件 `/home/ubuntu/flow-ui-browser-import.json`。样本包含开始、状态写入、结束三个节点，用于验证项目级仓库导入、待审核状态、审核、发布、从设计中心发起运行以及运行中心记录。

浏览器验收不使用测试套件的临时用户或项目；这些真实数据库测试对象会在测试结束时自动清理，以维持多用户数据隔离。

## 2026-08-18 真实页面闭环结果

在内部管理员会话中，已将上述 JSON 导入 `DEMO5A6A` 项目的根目录。导入后流程“浏览器验收状态流程”在流程设计中心显示为**状态流程 / 待审核 / 未发布**，证明仓库导入会受项目范围约束并创建真实项目流程，而非仅改变页面状态。

随后从项目流程设计中心审核通过该流程，并在独立设计器中发布。浏览器验证了仓库详情入口会精确加载“浏览器验收状态流程”（开始、写入验收状态、结束三个节点），而非错误回退到流程列表中的首条记录。

通过设计器“后端运行”发起后，系统自动跳转到已启动流程中心并显示运行 `8540ec66` 成功，耗时 `1347 ms`。节点运行日志显示开始节点写入 `stage: browser`、状态节点执行成功、结束节点完成，确认执行链路由服务端自主完成且结果持久化可审阅。

在修复仓库详情选择逻辑后，重新执行“流程仓库 → DEMO5A6A → 浏览器验收状态流程 → 打开设计器”。设计器准确展示该流程的**已发布 / 3 节点 / v3**状态与开始、写入验收状态、结束画布，没有回退到“原始控制流程示例”。

自动化真实数据库回归另验证了路由节点的多分支执行：`approved` 句柄仅执行开始、路由、通过分支与结束节点，明确不执行 `rejected` 分支。
