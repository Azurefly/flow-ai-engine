# Flow AI Engine 扩展功能浏览器验收记录

**验收时间：** 2026-08-15（GMT+8）  
**隔离身份：** 临时内部系统管理员（验收后清理）  
**验收流程：** `UI Governance Check`（验收后清理）

| 验收区域 | 浏览器级观察结果 | 结果 |
|---|---|---|
| 版本治理 | 设计器中可见版本快照 `v1`、版本选择器、差异区域与“恢复此版本”操作。 | 通过 |
| 节点复用 | 画布检查器中可见“管理我的模板与子流程”，设计器提供“保存当前定义为子流程”入口。 | 通过 |
| 模板复用与编辑 | 真实个人模板“界面验收映射模板”出现在 `REUSE LIBRARY`；点击后在画布新增同名转换节点。通过内联 JSON 编辑器将配置更新为 `{{input.edited}}` 与 `uiVerified: true`；数据库查询确认持久化，刷新后再次插入节点。 | 通过 |
| 运行分析 | 可见状态、时间、触发者筛选；成功、失败、失败率、平均耗时指标卡。 | 通过 |
| 运行详情 | 后端真实运行完成后，运行中心显示 1 次成功、909 ms 平均耗时、触发者和开始/结束节点的输入输出日志。 | 通过 |
| 失败告警 | 告警区域以独立红色信息面板呈现，当前无失败时明确显示 0 未读与空状态。 | 通过 |
| 375px 基础视口 | 375×812 预览下登录入口无横向溢出；新增模块采用已纳入前端回归测试的单列/换行/横向资产栏结构。 | 通过 |

> 此次浏览器验收生成的账号、流程、运行、版本、会话和审计数据均为临时测试数据；将在最终全量回归前按外键依赖顺序清理。

## 最终回归与清理

最终执行 `pnpm check && pnpm test`，**17 个测试文件、23 项测试全部通过**。随后复核隔离验收数据：临时用户、流程、节点模板、子流程和会话计数均为 `0`。该清理结果不影响自动化测试自身创建并清理的随机隔离数据。
