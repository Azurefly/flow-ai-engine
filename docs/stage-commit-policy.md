# 阶段性 GitHub 提交规范

公开代码仓库：<https://github.com/Azurefly/flow-ai-engine>

当前分支为 `main`。已将当前已验收工作区推送为首次同步提交 `917f7e2`（`chore: initialize public GitHub stage tracking`）。

后续每次完成一个可独立验收的阶段时，应依次完成回归验证、保存可恢复检查点、更新 `todo.md` 与验证台账，并创建包含该阶段代码与文档的独立 Git 提交后推送至 `github/main`。不得提交环境文件、密钥、会话 Cookie、生产凭据或用户私有数据。
