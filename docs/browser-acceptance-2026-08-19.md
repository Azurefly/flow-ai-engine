# 2026-08-19 浏览器验收记录

## 已完成页面检查

在开发预览中，浏览器已持久化为内部管理员会话。已验证以下真实界面状态：

| 页面 | 实际观察 | 结论 |
|---|---|---|
| 业务中心 | 顶部四页签“流程设计 / 已启动流程 / 流程仓库 / 系统配置”可见；业务中心标题、搜索框、项目表格和进入业务操作可见。 | 通过 |
| 新增业务 | 点击“新增业务”后，业务代号、业务名称、工作域、业务说明和“保存业务”表单字段实际展开。 | 通过 |
| 项目数据 | 显示现有验收项目与真实流程数量；未创建或伪造新的业务数据。 | 通过 |
| 项目工作区 | 进入 `DEMO5A6A` 后，真实加载四条流程；类型、审核、发布状态、搜索/三组筛选器以及详情、设计、发起操作全部可见。 | 通过 |
| 控制流程设计器 | 已发布的“P1 浏览器人工审批流程”真实显示开始、人工操作、结束节点；节点工具栏、禁用业务/物理资源、整理/导出/全屏/取消高亮工具、成员授权、运行输入和后端运行入口可用。 | 通过 |
| 流程详情 | 真实加载设计—审核—发布—运行引导、流程基本信息、一个已启动实例、版本快照、结构化差异与回滚入口。 | 通过 |
| 操作节点属性 | 选中“人工资料核验”后，真实显示操作代号、处理人方式、操作说明、指定处理人、帮助文案、未完全配置状态和高级 JSON 扩展区；字段值来自持久化流程定义。 | 通过 |
| 详情显式操作 | 流程详情的“查看详细配置”按钮真实将视图滚动至画布，并自动选择人工操作节点，显示字段化属性面板。 | 通过 |
| 实例详情操作 | 已启动流程表的“实例详情”真实打开受权限保护的运行日志弹层，加载开始、操作、结束节点的输入、输出及人工任务完成结果。 | 通过 |
| 系统配置 | 管理员真实可见系统配置左栏、收起导航控制、通用/审批/工作域/身份页签及通用设置持久化表单。 | 通过 |
| 系统配置导航状态 | 左栏收起后，图标导航仍可打开“审批配置”；审批发布门禁、要求审核通过开关和保存规则操作真实可见。 | 通过 |
| 已启动流程工作台 | 真实显示看板、日历、待办、已办、我发起和全部流程导航；日历中加载状态控制流程和人工审批流程的真实事件，统计数量随查询完成刷新。 | 通过 |
| 流程仓库 | 在 `DEMO5A6A` 项目下真实加载根目录、已发布目录与三条流程；搜索、批量操作、勾选、目录移动和只读预览入口均可见。 | 通过 |
| 仓库只读预览 | 选择“P1 浏览器人工审批流程”后，真实加载流程简介、类型、版本、只读节点画布、工具栏和“打开设计器”操作。 | 通过 |
| 数据资源中心 | 真实显示数据源、资源探查、UDF、标签、项目插件、数据流六个入口和数据源新增表单；页面明确只保存凭据引用且不返回明文秘密。 | 通过 |
| 数据流画布参考壳层 | 真实显示数据资源树、函数资源树、交集/差集/并集/TSML 的禁用状态、SQL/中间表配置入口、已启用计划、任务详情和一条成功的数据流运行审计输出。 | 通过 |
| 跨页导航 | 数据资源中心可返回业务中心并再次进入项目流程设计；控制流程仓库条目可重新打开既有节点画布，导航状态没有丢失。 | 通过 |
| 开始节点属性 | 选择真实开始节点后，属性面板显示“已配置”状态、初始变量 JSON、字段帮助和高级 JSON 配置；值与已执行实例中的 `vars.request` 一致。 | 通过 |
| 结束节点属性 | 选择真实结束节点后，属性面板显示“已配置”状态、结果模板 JSON、节点/变量模板提示和高级 JSON 区；模板与实例最终输出的 `decision`、`request` 字段一致。 | 通过 |
| REST 节点属性 | 在未保存画布状态中添加并选择 REST 节点后，真实显示请求地址、方法、请求头、请求体、超时与完整默认 JSON；面板明确提示服务端拒绝本机、私网、凭据 URL 和非标准端口，并限制超时范围。 | 通过 |
| 路由节点属性 | 在未保存画布状态中添加并选择路由节点后，真实显示路由规则数组、默认分支、`handle/label/condition` 约定、条件表达式字段和完整默认 JSON；状态为“配置中”，与执行器的连线句柄分派语义一致。 | 通过 |
| 业务中心筛选 | 真实显示关键词、创建时间开始、创建时间结束、查询和重置控件；三条已授权项目记录正常呈现。 | 通过 |
| 受管同步入口 | “同步 BDP 配置”在未配置外部连接时保持禁用；页面明确说明筛选只作用于已授权可见项目，未触发外部同步。 | 通过 |
| 项目工作区收起导航 | 进入 `DEMO5A6A` 后，收起左侧项目导航；代号缩写、图标化流程/权限/资源入口、完整流程列表与既有详情/设计/发起按钮仍可见。未点击会改变流程状态的操作。 | 桌面通过；窄屏待验收。 |
| 已启动流程收起导航 | 真实打开工作台后，默认导航、收起控制、看板、日历、待办、已办、发起与全部流程入口正常显示；标题下明确提示仅展示具备运行权限的实例和任务。收起后保留带提示的图标导航、真实看板统计以及一条待办/一条已办摘要；未领取、移交、退回或完成任何任务。 | 通过 |
| 取消发布治理 | 打开已发布控制流程的设计器后，真实显示“发布治理”区块、取消发布的运行阻断说明和“取消发布”控件；未点击确认，不改变流程发布状态。 | 入口通过；状态转换由真实数据库集成测试覆盖。 |
| 服务重启后流程列表 | 在审核重置服务热更新并重启后重新进入 `P0CBF7DE`；首个画面处于查询中，等待响应后真实显示已发布控制流程与待审核状态流程，审核、发布与设计/发起入口均可见。未触发任何状态变更。 | 通过 |
| 流程时间审计 | 进入已发布“状态控制流程”的真实设计器后，流程详情显示创建时间 `2026/8/18 02:58:59`、最近发布时间 `2026/8/18 02:59:07` 与“尚未取消发布”；字段来自 `createdAt`、`publishedAt`、`unpublishedAt`，未以更新时间替代。未点击取消发布或其他状态操作。 | 通过 |
| 流程设计中心筛选 | 在 `P0CBF7DE` 项目流程设计中心真实显示关键词、类型、审核和发布状态筛选，及独立“查询”“重置筛选”按钮；查询稳定后两条授权流程正常显示。未执行审核、发布、发起或数据修改。 | 通过 |
| 项目流程详情时间 | 打开已发布“状态控制流程”的项目详情弹层，真实显示创建时间 `2026/8/18 02:58:59`、最近发布时间 `2026/8/18 02:59:07` 与“尚未取消发布”；未进入设计器或触发任何状态操作。 | 通过 |
| 项目切换流程列表 | 开发预览重新进入 `P0CBF7DE` 项目后，等待流程列表请求完成；真实显示“状态控制流程”和“未发布流程”两条记录。此前初始加载阶段的空状态不再被误作最终验收结论；项目标识变化时筛选草稿与已应用筛选均会重置。 | 通过 |
| 流程仓库目录搜索 | 在 `DEMO5A6A` 仓库真实输入“已发布”；左栏保留匹配目录“已发布流程”，中间根目录流程区显示“没有搜到任何数据”。未执行导入、导出、移动、目录编辑或删除。 | 通过 |
| 结构化数据资源配置 | 在 `DEMO5A6A` 资源配置中心真实查看数据源与资源探查。数据源使用名称、类型、连接说明、地址/位置和凭据引用字段；资源探查使用字段名称/类型及样本键值对可重复行。两处均未显示 JSON 文本输入，未提交任何新资源。 | 通过 |
| 结构化运行字段 | 打开 `P0CBF7DE` 已发布控制流程的设计器；运行输入真实显示 `id` 与 `prompt` 的字段名/字段值行及“添加运行字段”，没有 JSON 文本框。未点击后端运行、保存或发布。 | 通过 |

> REST 与路由节点的浏览器面板检查完成后已刷新页面，未触发保存或发布；临时节点已丢弃，已发布的 P1 流程定义与版本没有改变。

刷新后已从服务端重新加载业务中心及三条项目记录，未见临时 REST 或路由节点造成的流程数量、版本或状态变更。

| 属性编辑稳定性 | 首次将 REST 请求方法由 `POST` 切换为 `GET` 时发现画布因草稿 JSON 长度作为组件 key 而重建，并进一步暴露定义回写与外部定义同步的循环。已移除草稿长度 key、区分本地回写和外部更新；再次在未保存节点执行 `POST → GET` 后，节点保持选中，完整字段面板仍可见，高级 JSON 同步为 `"method": "GET"`。 | 通过 |

> 以上字段编辑仅发生于未保存的临时 REST 节点。后续刷新将放弃该节点，不会保存或发布对已发布 P1 流程的任何改动。

> 节点工具库保持原始画布的拖拽添加方式；浏览器中的单击不会保存或发布临时节点，因此不会污染已发布验收流程。REST、路由和其他未现有画布节点继续由定义持久化与执行回归覆盖，待后续完成拖拽编辑验收。

## 后续浏览器闭环

下一步将使用现有验收项目验证流程详情、画布节点属性面板、仓库只读画布、系统配置左栏状态和数据资源调度入口。临时 `browser_audit` 内部账号仅用于该验收，完成后必须清理账户、会话和审计记录。
