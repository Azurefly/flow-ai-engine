# 2026-08-19 浏览器验收记录

## 已完成页面检查

在开发预览中，浏览器已持久化为内部管理员会话。已验证以下真实界面状态：

| 页面 | 实际观察 | 结论 |
|---|---|---|
| 业务中心 | 顶部四页签“流程设计 / 已启动流程 / 流程仓库 / 系统配置”可见；业务中心标题、搜索框、项目表格和进入业务操作可见。 | 通过 |
| 新增业务 | 点击“新增业务”后，业务代号、业务名称、工作域、业务说明和“保存业务”表单字段实际展开。 | 通过 |
| 项目数据 | 显示现有验收项目与真实流程数量；未创建或伪造新的业务数据。 | 通过 |
| 项目工作区 | 进入 `DEMO5A6A` 后，真实加载四条流程；类型、审核、发布状态、搜索/三组筛选器以及详情、设计、发起操作全部可见。 | 通过 |
| 控制流程设计器 | 已发布的“P1 浏览器人工审批流程”真实显示开始、人工操作、结束节点；节点工具栏、禁用业务/物理资源、整理/导出/全屏/取消高亮工具、成员授权、运行输入和后端运行入口可用。 | 通过 |
| 流程详情 | 真实加载设计—审核—发布—运行引导、流程基本信息、一个已启动实例、版本快照、结构化差异与回滚入口。 | 通过 |
| 操作节点属性 | 选中“人工资料核验”后，真实显示操作代号、处理人方式、操作说明、指定处理人、帮助文案、未完全配置状态和高级 JSON 扩展区；字段值来自持久化流程定义。 | 通过 |
| 详情显式操作 | 流程详情的“查看详细配置”按钮真实将视图滚动至画布，并自动选择人工操作节点，显示字段化属性面板。 | 通过 |
| 实例详情操作 | 已启动流程表的“实例详情”真实打开受权限保护的运行日志弹层，加载开始、操作、结束节点的输入、输出及人工任务完成结果。 | 通过 |

## 后续浏览器闭环

下一步将使用现有验收项目验证流程详情、画布节点属性面板、仓库只读画布、系统配置左栏状态和数据资源调度入口。临时 `browser_audit` 内部账号仅用于该验收，完成后必须清理账户、会话和审计记录。
