# AiFlowGraph 原始页面视觉差异审计

## 参考资产可用性

| 项目 | 结论 | 影响 |
|---|---|---|
| 项目共享目录中的 `AiFlowGraph.zip` | 当前沙箱中未找到该归档。 | 无法获得完整发布版的 CSS、打包脚本和图片资源，不能把裁剪副本的无样式渲染误认为最终视觉基线。 |
| 裁剪静态参考 | 保留 9 个 HTML 页面及部分图片引用；未保留 `assets/css` 文件。 | 可可靠提取页面分区、标题、工具栏、列表列、操作入口与状态文案；色彩、字号、间距只能通过当前源码线索和后续完整归档复核。 |
| `businessCenter.html` | 可证明项目中心标题、关键词/创建时间筛选、查询/重置、同步 BDP、导入/新增业务、业务列表列结构。 | 当前页面功能覆盖较多，但卡片化、渐变色导航和较大间距不应冒充原始布局。 |
| `design_process.html` | 可证明左侧流程/节点工具、中央画布、右侧配置面板、画布四项工具、配置状态图例和交互提示。 | 当前实现需按该三栏工作台而非通用仪表盘继续收敛。 |
| `businessOverview.html` | 可证明返回业务中心、业务选择上下文、流程设计中心和权限配置中心的左侧菜单与内容页签分区。 | 当前项目工作区已有这些功能，但需将视觉层级收敛为原始的窄左侧菜单和右侧内容工作区。 |
| `initiatedProcess.html` | 裁剪 HTML 不含可静态呈现的内容，动态菜单和看板由缺失脚本在运行时注入。 | 仅能依赖原始 HTML/脚本的结构线索、当前已恢复的功能和后续完整归档复核，不能虚构像素级外观。 |

## 本轮结论

原始页面在本地浏览器中的无样式渲染仍保留了可审计的 DOM 结构和原始文案：业务中心以筛选区、列表标题和表格为主；流程设计器由节点工具、画布工具、配置状态和右侧“配置信息”面板构成。由于完整 CSS 与安装包缺失，本轮将先以这些**可证明结构**约束高保真还原，并明确将色彩和像素级细节标注为“待完整归档复核”。

业务工作区原始结构可确认“返回业务中心 + 业务选择上下文 + 左侧模块菜单 + 右侧内容页签”的层级；已启动流程原始页面在裁剪副本中完全依赖缺失脚本，因此没有可呈现内容。后续视觉修复将优先消除当前控制台中与原始页面不相符的暗色品牌顶栏、营销式术语、过度圆角卡片和仪表盘化留白，使核心区域回归简约的流程管理工作台。

首次壳层调整后的真实开发预览已采用白色标题栏、蓝色选中页签、浅灰工作区背景和低圆角细边框，导航与四个授权项目均正常加载。仍可见“NEBULA BUSINESS ENGINE”等非原始品牌术语，以及业务中心中过度卡片化、标题区留白与原始列表密度不一致的问题；这些列为下一批壳层和业务中心修复项。

第二次真实浏览器核验显示，顶栏已不再呈现深色主题，并以“AI FLOW GRAPH”替代非原始品牌；业务中心也已收敛为白底、紧凑标题和扁平表格。进入 `DEMO5A6A` 后，项目工作区仍比原始 `businessOverview.html` 更像现代卡片控制台：左侧 256px 菜单、右侧较大标题区和圆角筛选条需要进一步压缩为原始的窄菜单、薄分隔线与紧凑工作内容区域。

项目工作区完成压缩后，真实页面已形成约 216px 的平面左侧菜单和连续右侧流程表格，真实四条流程数据、筛选与横向时间审计表仍可访问。进入数据流设计器后确认核心左仓库—中央画布—右配置面板仍完整，但顶部权限说明、协作成员、运行输入等多个卡片区挤占了原始画布工作台高度；原始 `design_process.html` 证据仅支持“节点工具 + 画布工具 + 配置状态 + 右侧配置面板”，因此下一步应把辅助信息收敛为可折叠次级区域，使画布优先可见。

画布壳层调整触发热更新后，工作区先显示一次临时空表，随后恢复四条真实流程；该现象属于既有授权查询加载过程，横向时间审计容器仍可用。当前工作区已满足原始的左右工作台层级，但“当前业务”和辅助说明仍较醒目，下一轮将继续把设计器辅助区收敛为可折叠状态，避免干扰画布主区域。

已启动流程工作台现以窄平面左侧视图菜单和无卡片间隙的右侧连续内容区呈现；首次进入时“正在读取已启动流程”遮罩明确显示，而不以零计数误导。当前内部管理员在该会话下没有可见任务或流程实例，故看板保持加载态；未创建额外流程实例以避免破坏多用户隔离与真实运行数据。

流程仓库已完成连续三栏视觉收敛：左侧目录树、中间根目录流程列表、右侧简介/只读画布预览以薄竖向分隔线相连。真实会话默认 `OPS` 项目无根目录流程时，三个区域分别呈现受权限约束的搜索树、空列表和未选中预览状态；项目选择器仍保留，未进行导入、导出或目录写入。

刷新后的 `OPS` 仓库读取到一条真实根目录流程，树、列表与右侧预览保持三栏对齐，批量操作和目录移动控件仍受既有项目编辑权限控制。随后已安全返回业务中心，为资源中心的真实数据视觉验收准备项目上下文。

`DEMO5A6A` 项目在授权查询完成后恢复四条真实流程；窄左侧菜单、紧凑筛选条、流程类型/审核/发布状态和横向时间审计列在视觉收敛后仍完整可访问。资源中心入口已准备就绪，未在本步骤触发任何流程、运行或调度写入。

资源中心在加载后呈现一条真实内联数据源和一条真实资源探查记录。左侧工作区菜单、资源标签、结构化数据源字段及运行审计的权限边界均保持；表单没有暴露 JSON 文本输入。资源中心的数据流分栏视觉规则已绑定为平面连续工作区，但本轮未切换数据流标签或触发运行、保存、调度操作。

随后切换到真实“数据流”标签，资源树、函数树、交集/差集/并集/SQL/TSML/中间表工具、保存图片与整理画布入口均以薄分隔的连续工作区呈现。真实运行审计显示三条既有记录，均为成功状态；页面仅做查看，未触发数据流运行、保存、暂停或删除托管计划。

## 同尺寸业务中心直接对照（1280px）

原始 `businessCenter.html` 在当前裁剪副本中可见的结构是：左上标题与图标、极简关键词/创建时间双行筛选、右上 BDP 同步、业务列表标题及紧凑七列表格。当前真实业务中心已经采用白底、薄灰表格与右侧筛选条，但仍保留顶层四页签、内部身份栏、业务工作域与流程计数等为安全多项目系统新增的必要信息；这些不是原静态页面的像素复刻。当前开发版首屏连接状态已改为白色细边框加载反馈，不再出现深色全屏仪表盘背景。后续视觉工作将以保留安全必需信息为前提，继续压缩额外文案与标题高度。

业务中心标题前的非原始流程标识已隐藏，标题栏、筛选容器与业务列表外框均收敛为低密度白色连续表格。真实浏览器仍显示 4 个已授权业务和查询/重置/进入业务操作；未修改任何业务数据。

设计器视觉复验进入 `DEMO5A6A` 后，首次绘制暂时显示空流程表；该页面已有项目切换后异步查询的已知加载过程，不能将该短暂状态作为功能或视觉结论。后续只在真实流程记录加载完成后打开设计器，并保持只读浏览。

刷新开发预览后，认证等待阶段呈现白色页面与细边框加载反馈；授权查询完成后恢复 4 个真实业务项目。该过渡不再出现深色全屏加载页，且未影响业务中心的筛选或可见项目清单。

再次进入 `DEMO5A6A` 后，授权流程列表恢复 4 条真实记录；列表中的状态、审核、时间审计和设计入口可用。该加载后基线将用于验证设计器默认收起左侧流程仓库栏的主画布模式，验收不触发保存、发布或运行。

打开已发布数据流后，流程仓库栏默认收起，屏幕主区域由流程标题、操作条、结构化运行字段、双侧工具条、画布和配置信息面板组成。真实画布显示开始与结束节点、配置状态图例、画布操作提示与只读配置空状态；未点击保存、发布、运行、取消发布或修改任何节点。浏览器可访问树仍保留被收起的流程列表，使顶部导航按钮能够随时恢复流程选择功能。

## 原始视觉资产复检

本轮重新扫描项目共享目录、`/home/ubuntu/reference-aiflow/original/AiFlowGraph` 裁剪副本和 `/home/ubuntu` 下的归档候选，均未发现 `AiFlowGraph.zip`、原引用的 `assets/css/*.css`、图标 PNG、`designProcess/bundle.js` 或 `canvas/canvas.js`。因此现阶段可使用的唯一原始视觉证据仍为静态 HTML 中的 DOM 层级、类名、文字、按钮与页面分栏；不能将当前 CSS 推断为原项目像素级样式。后续需要完整归档或缺失 assets 才能继续完成精确字体、间距、图标和状态皮肤还原。

## 已启动流程连续工作台复验

真实内部会话进入“已启动流程”后，页面先显示白色加载遮罩，随后过渡为左侧视图导航和右侧连续内容区。稳定状态读取到 1 个待办、1 个已办、1 个我发起和 2 个全部可见实例；最近任务列表中显示真实任务名、发起人、时间和状态。侧栏的“我的看板、日历、待办、已办、我发起、全部流程”入口保持可访问。全程仅查看，没有领取、完成、移交、退回或修改任何任务。

## 项目流程查询状态复验

进入 `DEMO5A6A` 的首帧曾显示“项目内暂无匹配流程”，随后授权查询返回四条真实流程并正确显示审核、发布、创建、最近发布与最近取消发布时间。该现象证明当前列表仍需在查询期显示明确的加载反馈，不能把未返回的数据渲染为空状态；已登记为独立还原待办。本轮没有点击详情、设计、发起、审核或发布操作。

## 流程设计器主画布复验

在 `P2 浏览器订单数据流` 的已授权真实版本中，稳定状态显示左侧流程仓库、顶部导入/导出/保存/发布操作、运行字段表单、纵向节点工具栏、画布状态图例和右侧配置检查器。开始、结束、REST、SQL、转换、条件、LLM、子流程、HTTP、资源、数据流中间节点、UDF 与输出节点入口均可见；运行输入由“字段名 / 字段值”的重复行组成，未向用户暴露 JSON 文本框。画布节点维持开始—结束连线，右侧流程引导、发布治理和版本治理均与当前数据流的真实版本状态一致。全程未点击保存、发布、取消发布、后端运行、成员授权、删除、恢复版本或任何节点新增操作。

## 系统配置连续工作台复验

系统配置页采用原安装包式左侧模块导航和右侧连续内容区域；通用设置显示平台名称、水印状态和字段化编辑区域，审批配置显示发布审批说明、结构化开关与服务端门禁提示。导航的收起入口和通用设置、审批配置、工作域配置、身份与权限页签均可访问。所查看的管理员配置全部使用文本框、复选框或表单控件，没有向用户显示 JSON 文本框；本轮未保存任何系统设置或更改审批规则。

## 控制台加载反馈复验

刷新已登录开发预览时，控制台先呈现白色细边框的“正在读取流程工作台”反馈，授权数据到达后恢复为真实的 4 个业务项目及其筛选和进入业务操作。该过程没有渲染业务空列表，也未触发任何项目、流程或调度写入。

进入 `DEMO5A6A` 项目时，项目工作区先呈现“正在读取项目流程 / 正在加载当前业务授权范围内的流程与审核状态”，随后显示 4 条真实流程及完整审核、发布和时间审计列。该实测覆盖了项目流程查询加载遮罩，不再将未返回的数据视为“暂无匹配流程”。

## 数据资源中心结构化表单复验

在 `DEMO5A6A` 的真实资源中心中，资源读取完成后显示 1 个已校验的数据源、1 个资源探查记录和 1 个数据流。数据源录入区域仅显示名称、类型、连接说明、服务地址/文件位置和凭据引用等普通字段；没有 JSON 文本框、JSON 编辑提示或明文凭据入口。全程未保存、删除或修改资源。

## 剩余静态页面证据提取

`system_config.html` 可证明其为“左侧可收起导航 + 右侧 `nav-card-tabs` 页签容器”的双栏布局；`process_detail.html` 可证明流程引导、基本信息、流程图、已启动流程列表按虚线分隔标题顺序排列，流程图标题行包含“查看详细配置”“取消发布”，画布内含取消高亮、整理画布、保存为图片与全屏入口。`dataFlowCanvas.html` 可证明独立的左资源树/函数树、右侧顶部业务名与“一键展开、任务详情、调度配置、保存画布”工具条、画布工具条及可收缩配置区结构。对应 CSS、图片、脚本均仍缺失，因此这些记录仅作为可证明 DOM 与可见操作基线，不能推断原始像素级配色和尺寸。

最近一次重新扫描确认：项目共享目录当前不含可读取文件，`/home/ubuntu` 下未发现 `AiFlowGraph.zip`、`designProcess/bundle.js`、`canvas/canvas.js` 或原安装包 `assets/css/*.css`。因此节点原始字段、图标、精确字号和像素级间距仍无法从现有材料证明；本项目继续只依据 9 个保留 HTML 页面的可见结构和操作文案进行收敛。

稳定视觉标记热更新后，已登录控制台仍先显示“正在读取流程工作台”加载反馈，再恢复 4 个真实业务项目及筛选、同步和进入业务操作。未出现授权查询完成后的空列表，且未触发任何数据写入。

系统配置在稳定 `data-aiflow-system-config` 标记下呈现连续左导航、右侧卡片页签与通用设置内容区；平台名称、水印启用与水印文字仍为普通字段控件。此次核验仅查看，没有保存任何设置。

本轮进入流程详情前，控制台再次先显示“正在读取流程工作台”，授权完成后恢复 4 个真实业务项目。该异步过渡只作为加载反馈验证，不将首帧空表视为业务空状态。

进入 `DEMO5A6A` 后，项目加载遮罩结束并显示 4 条真实流程及其类型、审核、发布、创建、发布/取消发布时间与操作列。后续流程详情核验将在这些真实受权数据上只读进行，不执行发起、保存、审核或发布。

点击已发布数据流的“详情”后，项目工作区显示 `PROCESS DETAIL` 弹层，包含流程类型、审核状态、发布状态、定义版本、创建时间、最近发布/取消时间、说明与“进入设计器”入口。弹层仅用于项目层基本信息预览；本轮未点击进入设计器、关闭以外的写入性操作，也未改变流程状态。

从详情进入 `P2 浏览器订单数据流` 设计器后，主画布保持开始—结束连线、纵向节点工具、整理画布/保存为图片/全屏/取消高亮工具、配置状态图例与右侧检查器。画布下方的 `PROCESS DETAIL` 同时显示四步流程引导、基本信息、三项时间审计、已启动流程列表和版本治理；当前数据与项目详情一致。全程未保存、发布、取消发布、运行、成员授权、恢复版本或修改节点。

流程详情热更新后，主画布、权限成员区、结构化运行输入、节点工具、检查器、流程引导和版本治理均保持可见；实例查询完成后显示真实稳定的空状态“当前关键词下尚无已启动流程”。新增加载行仅在查询中显示，不覆盖稳定空状态。

本轮转入已启动流程工作台前，控制台先呈现“正在读取流程工作台”，随后恢复 4 个真实授权业务项目。加载反馈与业务中心表格均正常，无写入性操作。

已启动流程工作台真实加载后保留可收起左导航与右侧看板内容区：待办 1、已办 1、我发起 1、全部可见 2，最近任务显示一条待处理和一条已办记录。加载遮罩在数据可用后消失；本轮仅查看，没有领取、完成、移交、退回、批量处理或刷新任务。

已将工作台连续双栏规则从泛化 `aside` 结构选择器收敛为以“收起/展开已启动流程导航”稳定 aria 标记锚定的规则。热更新后在同一真实会话复验，导航、统计和最近任务未回退；该调整不修改任务数据或运行状态。

本轮流程仓库验收前，业务中心同样由授权查询首帧的空表恢复为 4 个真实业务项目；仅以此验证加载恢复，不做业务写入。

在 `DEMO5A6A` 仓库中，真实目录树显示根目录 3 条流程与“已发布流程”目录；选择 `P2 浏览器订单数据流` 后，右侧显示流程简介、只读开始—结束画布、打开设计器、整理画布、保存为图片、全屏展示、取消高亮、配置状态与检查器。该结构覆盖原始 `processWarehouse.html` 可证明的树、简介、画布与工具条；本轮未导入、导出、移动目录、打开设计器、保存图片或改变流程定义。

`businessCenter.html` 可证明业务中心包含图标标题、关键词与创建时间范围、查询/重置、业务列表计数、同步 BDP 配置、导入业务、新增业务以及列表操作列。当前业务中心已覆盖标题、关键词、日期范围、查询/重置、同步受管入口、新增业务与连续表格；原始业务导入的脚本与文件格式未保留，因此不能推断其数据契约或安全地复刻导入行为，仍作为证据缺口保留。

原始 `assets/js/index.js` 进一步证明顶层导航按顺序映射至业务中心/项目工作区、已启动流程、流程仓库、系统配置；当前控制台的四页签保持同一信息架构。该脚本不包含“导入业务”的文件解析或后端字段契约，不能据此声称任何特定原始导入格式。

业务中心真实管理员会话现显示“同步 BDP 配置 / 导入业务 / 新增业务”连续操作组；导入入口提示列式 CSV 的业务代号、业务名称、可选工作域代号和业务说明。原始格式不可证明，当前 CSV 契约作为安全兼容扩展实现；本轮仅验证入口可见，未选择文件、未创建或写入业务数据。

数据流页的真实资源中心验收中，结构化数据源字段、资源树、函数树、任务与调度入口、画布工具栏、独立画布辅助工具、运行审计和托管计划状态均正常可见。辅助工具区的自定义属性已改为有效字符串值；本轮未运行、暂停、删除、保存调度或修改资源。

移除数据资源中心未挂载的旧 JSON 代码后，DEMO5A6A 的真实数据源页稳定显示普通字段输入（名称、类型、连接说明、地址/位置、凭据引用）以及既有“P2 浏览器订单源”校验记录；未出现 JSON 文本框。资源探查页继续由字段结构和样本键值行承载。全程未保存或删除资源。

资源探查真实稳定状态显示资源名称、可选数据源、资源类型、字段结构行和一行样本键值行，并保留既有“P2 浏览器订单样本”的类型、字段/样本数量与状态；页面不提供 JSON 文本输入。

已启动流程工作台真实待办抽屉在读取后正常显示任务说明、可分配处理人、移交和领取入口；未出现旧“处理结果（JSON）”文本区域。待办保持未领取状态，未执行移交、领取或完成。当前处理结果的结构化字段行仅在任务领取后按状态机呈现，已由源代码和既有回归保护。

## 数据流工作区连续分栏复验

真实数据流页显示资源树、函数树、交集/差集/并集/SQL/TSML/中间表工具、画布辅助工具、已操作流程列表、数据流列表与调度草稿。审计表包含三条既有成功记录，其中两条为托管调度、另一条为手动运行；生产计划在页面中显示“已启用”。本轮只查看资源、运行审计与工具入口，未点击运行、暂停计划、删除计划、保存草稿、打开设计器或查看审计详情。

系统配置最新页面维持左侧模块菜单、右侧卡片页签条和无额外阴影的连续内容区；通用设置的名称、水印开关和水印文字仍为字段化控件。未保存或修改任何系统设置。

认证首屏已移除遗留的“NEBULA INSPIRED · V3”深色仪表盘样式，改为浅灰工作区、顶部蓝色细线、白色细边框登录面板和与控制台一致的 `AI FLOW GRAPH` 标识。开发预览在认证查询短暂加载后正常显示用户名、密码、登录按钮和无公开注册提示；本轮未提交登录凭据。

在 `375×812` 窄屏预览中，浅色登录面板完整显示品牌标识、平台标题、说明、用户名与密码输入、登录按钮和无公开注册提示；未见横向溢出或控件裁切。

在 `DEMO5A6A` 的真实 `P2 浏览器订单数据流` 设计器中，运行输入区域仅显示 `id`、`prompt` 两行字段名和值、删除行按钮及“添加运行字段”；浏览器可访问树中不含旧 JSON 文本输入。后端运行按钮、节点工具、画布和配置面板保持可见。本轮未保存、发布、运行、授权或修改流程。

移除系统配置泛化结构选择器后，真实管理员配置页仍以 `data-aiflow-system-config` 呈现连续左侧模块导航、右侧页签和字段化通用设置；平台名称、水印状态、普通输入、复选框和保存按钮均可见，未出现 JSON 输入或布局回退。本轮未保存系统设置。
