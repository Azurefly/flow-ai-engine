# P2 浏览器验收记录

## 资源中心页面基线

在内部管理员会话中进入 `DEMO5A6A` 原始工作区验收项目，已从项目工作区左侧导航打开**资源配置中心**。页面展示了参考原始项目资源模块的信息结构：数据源、资源探查、UDF、标签、项目插件和数据流六个入口；初始资源数量均为 0，并显示项目隔离的数据源表单、连接元数据 JSON 编辑器及空列表状态。

资源中心文案明确说明页面不会返回明文凭据。该项目作为浏览器 P2 验收目标；后续步骤会创建内联数据源和资源样本，验证数据流设计、手动运行和调度草稿。

随后已在同一项目创建内联数据源“P2 浏览器订单源”，列表显示其类型为 `inline`、状态为“已校验”，并只显示描述性元数据。资源探查页将“P2 浏览器订单样本”绑定到该源，列表返回 `dataset`、1 个字段、1 行样本、启用状态，证明数据源—资源外键与项目范围在真实页面生效。

已从流程设计中心创建“P2 浏览器订单数据流”。设计器工具栏包含资源、中间表、筛选、字段映射、只读 SQL、UDF 与输出节点。创建后发现异步详情到达时画布保留上一控制流程节点；本轮已修复为按流程 ID 和定义版本重挂载画布，下一步将从项目流程表重新打开该数据流进行回归复验。

## 数据流生命周期与专属运行

从项目流程表重新打开“P2 浏览器订单数据流”后，画布正确显示该数据流自己的开始—结束两个节点，没有残留上一条人工审批流程的节点。管理员在项目流程设计中心审核通过该流，再在设计器发布至 `v2`。

通过设计器“后端运行”发起已发布数据流。页面未跳转至通用运行中心；数据库的 `dataflow_run` 审计表新增运行 `tzgZk4gW`，状态为 `success`，开始和结束时间均为 `2026-08-18 08:12:03`，无错误字段。这证明设计器的 data 类型运行入口已经分派到 P2 专属执行服务。

## 托管调度草稿

在资源中心“数据流”页，P2 数据流运行审计展示了 `tzgZk4gW · manual · 成功` 以及开始、结束节点的结构化输出。为该发布流保存六段 UTC 表达式 `0 0 9 * * *` 后，项目数据库写入调度 `m2N0UDNT`，状态为 `paused`，未配置平台任务 UID。刷新页面后，数据流表格稳定显示“草稿 / 暂停 · 0 0 9 * * *”。

调度草稿只保存项目级计划与授权审计，不会创建后台任务。实际启用需先保存稳定检查点并由项目所有者发布部署版本；届时平台回调将只按可信计划 UID 关联调度记录。
